# Milestone 2.3 -- Execution Log

## 2026-08-08 (start)
Did: Breakdown Milestone 2.3 via skill `planning-and-task-breakdown`. Sebelum breakdown task, riset 2x `WebFetch` ke dokumentasi resmi GCP menemukan BigQuery Sandbox mode (project `nirwana-database-elt`) punya hard limit 60 hari expirasi untuk SEMUA tabel/view/partition, tidak bisa dioverride per-tabel -- jauh lebih keras dari yang diketahui saat insiden M2.1 (yang hanya soal partition). Ini kontradiksi langsung dengan syarat "full history" M2.3. Diajukan ke user via `AskUserQuestion`: billing GCP diaktifkan sekarang? -- jawaban awal "ya", tapi user lalu koreksi: belum bisa karena kendala kartu kredit saat ini, akan diaktifkan di masa depan. Plan direvisi total mengikuti arah "jalan dulu tanpa billing + mitigasi ringan". 2 keputusan lain diajukan sekaligus: mekanisme DQ gate blocking (dipilih: build ke schema terpisah -> test -> swap) dan watermark incremental (dipilih: dbt native `is_incremental()`, bukan reuse cursor M2.1). User juga minta breakdown 14 task dipecah jadi checkpoint (commit+log tiap checkpoint), bukan cuma 1 checkpoint di tengah.
Result: worked. `decisions.md` ditulis lengkap. Entri baru ditambahkan ke `docs/keputusan-tertunda.md` ("Aktivasi billing GCP...") mendokumentasikan gap project-wide ini (bukan cuma M2.3) beserta rencana migrasi begitu billing aktif.

## 2026-08-08 -- Fase 1: Fondasi (Task 1-3)
Did: Tulis `scripts/extract/renew_expiration.py` (perpanjang `expirationTime` ~55 hari dari sekarang, untuk semua tabel/view di satu/lebih dataset). Uji lokal di `raw_production` (23 tabel, pakai kredensial `extract-writer`) dan `staging` (24 objek termasuk seed `department_mapping`, ternyata berhasil juga meski `extract-writer` tidak secara eksplisit diberi akses dataset `staging` -- kemungkinan lewat grup `projectReaders`/`projectWriters` bawaan, tidak diselidiki lebih jauh karena hasil akhirnya benar). Tambahkan step baru ke `.github/workflows/extract-production.yml` (hanya untuk `raw_production` -- scope kredensial `extract-writer` di workflow itu). Trigger manual, verifikasi run sukses & expirationTime benar-benar berubah (dicek numerik, bukan cuma "job sukses"). Buat dataset `mart_cleaned` + `mart_cleaned_staging` (region US) -- `dbt-transform` sudah punya akses otomatis (project-level `dataEditor` dari M2.2, tidak perlu ACL tambahan).
Result: Task 1-3 selesai & terverifikasi. Keputusan "tidak perlu layer intermediate" (Task 2) murni dokumentasi -- sudah final di `pemetaan-kebutuhan-konsumen-data-mart.md`, tidak ada implementasi yang perlu dijalankan.

## 2026-08-08 -- Fase 2: Mekanisme Gate + Model Percobaan (Task 4-5) -- ditemukan blocker fundamental, desain diubah
Did: Tambah target dbt baru `mart_staging` di `profiles.yml` untuk build ke dataset `mart_cleaned_staging` -- ternyata BUG: `ref()` model staging ikut resolve ke dataset target aktif (bukan tetap ke "staging"), karena schema staging tidak di-pin eksplisit. Diperbaiki total: hapus multi-target, tambah `macros/generate_schema_name.sql` (override supaya custom `+schema` dipakai literal, tidak digabung dengan dataset target -- akar masalah yang sama juga menyebabkan bug "staging_staging" di M2.2), set `+schema: mart_cleaned_staging` eksplisit untuk folder `models/mart_cleaned/`. Tulis model percobaan `mart_cleaned__bookings.sql` dengan `incremental` + `is_incremental()` + strategi `merge`. Full load pertama sukses (217,7k baris, CREATE TABLE/DDL). Run kedua (seharusnya cuma proses delta lewat MERGE) **gagal total**: "DML queries are not allowed in the free tier."
Result: **Temuan kritis baru** -- BigQuery Sandbox mode ternyata memblokir SEMUA DML (bukan cuma soal partitioning yang sudah diantisipasi), termasuk strategi `append` dbt. Tidak ada mekanisme incremental dbt manapun yang bisa jalan tanpa billing. Dikonfirmasi ke user (tidak ada opsi kedua yang genuinely berbeda selain full refresh, jadi tidak diajukan sebagai pilihan formal) -- lanjut dengan **full refresh murni** (`materialized: table`, `CREATE OR REPLACE TABLE`, DDL) untuk `mart_cleaned`. Model `is_incremental()` dibiarkan di kode (dormant, otomatis False saat materialized=table) untuk diaktifkan lagi begitu billing aktif -- tidak perlu ditulis ulang nanti.
Pelajaran: constraint Sandbox mode jauh lebih luas dari yang terlihat di insiden M2.1 (partition-only) -- perlu diuji EMPIRIS (run 2x) untuk setiap mekanisme baru yang menyentuh BigQuery di project ini, bukan diasumsikan dari 1 kali percobaan sukses.

## 2026-08-08 -- Build->test->swap script (Task 4, lanjutan)
Did: Tulis `scripts/mart_cleaned/promote.py` (`dbt run` -> `dbt test` -> `CREATE OR REPLACE TABLE` per tabel ke `mart_cleaned`, HANYA kalau semua test lolos). Percobaan pertama gagal: `get_client()` dari `scripts/extract/bq.py` pakai kredensial `extract-writer` (scope dataset `raw_production` saja), tidak punya akses ke `mart_cleaned_staging` -- `403 Forbidden`. Diperbaiki: `promote.py` pakai client terpisah dengan kredensial `dbt-transform` (env var baru `DBT_TRANSFORM_CREDENTIALS`), bukan reuse helper `bq.py` yang memang didesain untuk `extract-writer`.
Result: worked. Uji 2 skenario terkontrol -- (1) data bersih: `dbt test` 3/3 PASS, `mart_cleaned__bookings` ter-promote, row count `mart_cleaned` = 217.654 (cocok staging). (2) tes sengaja dibuat gagal (`tests/_temp_always_fails.sql`, dihapus setelah diverifikasi): `dbt test` FAIL, script berhenti SEBELUM promote, `COUNT(*)` di `mart_cleaned` dicek ulang -- tetap 217.654, tidak berubah sama sekali. Gate terbukti benar-benar blocking, bukan cuma laporan setelah fakta.

## Status saat ini
Checkpoint 2 selesai. Lanjut Fase 3 (Task 6-11: 23 model mart_cleaned per schema).
