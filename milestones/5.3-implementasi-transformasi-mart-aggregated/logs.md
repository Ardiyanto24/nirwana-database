# Milestone 5.3 — Execution Log

## 2026-08-08 — Checkpoint 1: Setup + 27 Dimension Table (Task 1-2)
Did: Tambah blok `mart_aggregated` di `dbt_project.yml` (+schema mart_aggregated_staging, +materialized table). Scaffold folder hybrid (`corporate_master`, `reservation_revenue`, `fnb_operations`, `facility_maintenance`, `spa_event`, `hr_finance/{hr,corporate_financial}`). Implementasi 27 model dimension table + `_mart_aggregated_dimensions_tests.yml` (65 test: unique/not_null tiap PK, accepted_values untuk kategori dengan nilai pasti, relationships untuk FK lintas dimension).
Result: `dbt debug` OK (koneksi BigQuery valid). `dbt run --select mart_aggregated` sempat 1 error: `dim_ingredient` referensi kolom `ingredient_name` yang ternyata tidak ada di `mart_cleaned__ingredient_price_history` (dicek langsung via `client.get_table()` -- skema aktual cuma `ingredient_id` STRING, `unit_cost`, `date`, `_synced_at`). Dikoreksi: `dim_ingredient` cuma pakai `ingredient_id` (berperan ganda sebagai nama). Koreksi juga didokumentasikan di `DataSchema-mart-aggregated.md` (bukan diperbaiki diam-diam). Re-run: 27/27 tabel berhasil dibuild. `dbt test`: 65/65 PASS, 0 error.

## 2026-08-08 — Checkpoint 2: Fact Table Revenue (Task 3)
Did: Sebelum menulis SQL, cek skema aktual `bookings`/`daily_occupancy`/`pricing_history`/`guests`/`financial_summary` via `client.get_table()` -- ditemukan 2 flaw grain di desain M5.2: (1) `fact_revenue_daily` draf M5.2 (grain property/room_type/channel/date) tidak valid karena `daily_occupancy` tidak punya kolom `booking_channel` sama sekali, sementara metrik channel (cancellation/no-show) grain aslinya property/channel tanpa room_type -- dipecah jadi `fact_revenue_room_type_daily`, `fact_revenue_channel_daily`, `fact_revenue_los_daily` (satu-satunya yang genuinely butuh room_type+channel), dan metrik property-level (growth/lead time/repeat guest/ranking) digabung ke `fact_revenue_property_daily` (menggantikan `fact_revenue_property_summary`). (2) `gop_pricing_impact` butuh `financial_summary.gop` yang grainnya BULANAN (`period` STRING 'YYYY-MM'), dipaksakan ke tabel harian akan mengulang nilai bulanan yang sama di 30 baris tanpa makna -- dipisah jadi `fact_revenue_gop_impact_monthly` (2 kolom: `avg_pricing_deviation`, `gop_margin`, bukan 1 kolom "impact" yang tidak bisa dihitung bermakna dari 1 baris). Kedua koreksi didokumentasikan eksplisit di `DataSchema-mart-aggregated.md` (bukan diperbaiki diam-diam). Implementasi akhir: 9 fact table Revenue (naik dari rencana 5+pace booking di plan awal, karena pemecahan grain di atas), termasuk `fact_revenue_pace_booking_snapshot` (self-union `CREATE OR REPLACE` via `{{ this }}` + cek `adapter.get_relation`, idempoten untuk re-run hari yang sama).
Result: Semua 9 tabel berhasil dibuild. `fact_revenue_pace_booking_snapshot` menghasilkan **0 baris** -- dicek dan dikonfirmasi BUKAN bug: dataset sintetis proyek ini punya rentang tetap sampai `max(check_in_date)=2026-07-01`, sedangkan tanggal BigQuery hari ini `2026-08-08` (query `current_date()`) -- tidak ada booking "masa depan" relatif terhadap hari ini karena dataset statis, konsisten dengan karakteristik "freshness lag" yang sudah didokumentasikan project-wide di `CLAUDE.md`. `dbt test`: 21/21 PASS, 0 error.

## 2026-08-08 — Checkpoint 3: Fact Table F&B (Task 4)
Did: Cek skema aktual `fnb_transactions`/`fnb_inventory`/`recipe_bom`/`fnb_waste_log` via `client.get_table()`. Temuan penting: `fnb_transactions.transaction_id` BUKAN row-level unik (~2.33 baris/transaksi, multi-item order -- sudah diperingatkan `warehouse/README.md`) -- `transaction_count`/`walk_in_ratio` di `fact_fnb_outlet_daily`/`fact_fnb_customer_type_daily` dihitung dari `COUNT(DISTINCT transaction_id)` lewat CTE `tx_level` (SELECT DISTINCT), bukan `COUNT(*)`. `fnb_inventory` ternyata tidak punya kolom tanggal sama sekali (tabel current-state, bukan histori) -- `fact_fnb_inventory_status` pakai `CURRENT_DATE()` sebagai penanda snapshot, konsisten catatan M5.1 "snapshot terkini". `food_cost_ratio_target` (34%/24%/28% Food/Beverage/Dessert) dihardcode dari role-play dokumen Data Analyst §2.1 -- bukan turunan tabel manapun, dicatat eksplisit di komentar SQL. Implementasi 8 fact table: outlet_daily (+capture_rate cross-domain dari daily_occupancy), category_daily, hourly, customer_type_daily, menu_item_daily (+food cost cross-table recipe_bom x ingredient_price_history), waste_daily, inventory_status, ingredient_price_daily.
Result: Semua 8 tabel berhasil dibuild. `dbt test`: 19/19 PASS, 0 error.

## 2026-08-08 — Checkpoint 4: Fact Table Facility/Ops (Task 5)
Did: Cek skema `rooms`/`housekeeping_log`/`maintenance_tickets` via `client.get_table()`. 2 koreksi ditemukan: (1) `mart_cleaned__rooms` tidak punya kolom tanggal (current-state, sama seperti `fnb_inventory`) -- `fact_facility_room_status_daily` pakai `current_date()` snapshot, kolom `out_of_order_hours` DIHAPUS (tidak ada sumber histori durasi). (2) Kolom "delayed_rate_vs_occupancy" dikoreksi jadi 2 kolom terpisah (`delayed_rate`, `occupancy_rate`) -- pola sama koreksi `gop_pricing_impact` di Revenue. Implementasi 9 fact table: room_status (snapshot), housekeeping room_type/property/staff (durasi dari `TIME_DIFF(cleaning_end_time, cleaning_start_time, MINUTE)`), maintenance ticket (SLA mentah tanpa flag breach, konsisten Keputusan #7), cost (grain `resolved_date`, cost baru final setelah resolved), room_recurrence & property_benchmark (tahunan, `building_age_years` dari `dim_property.opening_date`), technician.
Result: Semua 9 tabel berhasil dibuild. `dbt test`: 20/20 PASS, 0 error.

## 2026-08-08 — Checkpoint 5: Fact Table Spa & Event (Task 6)
Did: Implementasi 6 fact table (spa_daily, spa_customer_type_daily, spa_service_daily, event_venue_daily, event_property_daily, event_type_daily). 1 koreksi: kolom "low_utilization_streak_days" (consecutive days) disederhanakan jadi rolling count 30 hari terakhir via window function (`SUM() OVER (... RANGE BETWEEN 29 PRECEDING AND CURRENT ROW)`) -- gaps-and-islands eksak untuk streak murni di luar cakupan waktu M5.3, threshold `utilization_rate < 30%` didokumentasikan eksplisit sebagai asumsi bisnis (tidak ada nilai baku).
Result: Semua 6 tabel berhasil dibuild. `dbt test`: 13/13 PASS, 0 error.

## 2026-08-08 — Checkpoint 6: Fact Table HR + Watchlist (Task 7)
Did: Cek `staff_shifts`/`employee_performance`/`employees` -- 2 koreksi grain ditemukan: (1) `employee_performance.review_period` grain aslinya SEMESTERAN (`'2025-S1'`, dst, dikonfirmasi query distinct), bukan bulanan -- `latest_performance_score` dipisah dari `fact_hr_employee_monthly` ke 3 tabel semester baru (`fact_hr_employee_performance_semester`, `fact_hr_performance_department_semester`, `fact_hr_performance_by_status_semester`). (2) `employees` tidak punya kolom tanggal resign/terminasi (cuma `status` akhir) -- `fact_hr_turnover_monthly` tidak mungkin dihitung sebagai tren bulanan historis, diubah jadi `fact_hr_turnover_snapshot` (current-state, `mom_growth`/`yoy_growth` dihapus). `overtime_hours` dihitung `TIME_DIFF(clock_out,clock_in,MINUTE)/60-8`, dengan penanganan shift lintas tengah malam (+24 jam kalau `clock_out < clock_in`). Watchlist HR (kasus khusus kategori B) dibangun di checkpoint ini juga (folder sama `hr_finance/hr/`, bukan checkpoint terpisah seperti pace booking yang sudah digabung ke checkpoint Revenue) — `baseline_*` dihitung via window function expanding average (rata-rata seluruh bulan SEBELUM bulan berjalan per karyawan), tanpa kolom flag threshold (Keputusan #7). Implementasi akhir: 8 fact table HR (naik dari rencana 6+watchlist karena pemecahan grain semester).
Result: Semua 8 tabel berhasil dibuild. `dbt test`: 18/18 PASS, 0 error.

## 2026-08-08 — Checkpoint 7: Fact Table Corporate/Financial (Task 8)
Did: Cek `financial_summary`/`payroll` -- 1 temuan besar: `undistributed_expense` cuma **1 kolom total** di sumber, **tidak ada breakdown per komponen** (Admin&General/Sales&Marketing/Utilities/Property Maintenance/IT) sama sekali -- asumsi breakdown 5-komponen di M5.1/M5.2 ternyata dari narasi role-play "laporan USALI formal" (deskripsi umum industri), bukan dari skema data aktual. Ini seharusnya sudah tertangkap sebagai gap di M5.1, baru ketahuan sekarang -- dicatat eksplisit di `DataSchema-mart-aggregated.md` dan akan dicatat ulang di Known Gaps `report.md`. Kolom diubah jadi `undistributed_expense_total` tunggal. Ditangani juga aturan `warehouse/README.md`: baris P&L nyata ada di `department='Overall'` (P01-P05) DAN `department='Corporate Overhead'` (khusus P06) -- filter `department IN ('Overall','Corporate Overhead')` menangkap keduanya dengan benar (tiap property cuma punya salah satu). 1 koreksi grain: `fact_financial_service_charge_daily` diganti nama jadi `fact_financial_service_charge_monthly` -- `payroll.period` grain aslinya bulanan (dicek langsung, format `'2023-07'`), bukan harian, occupancy_rate di-roll-up bulanan juga supaya grain cocok. Implementasi 9 fact table: business_line_monthly, overall_monthly (dengan MoM/YoY self-join), revenue_runrate_daily (agregasi lintas 4 sumber: bookings+fnb_transactions+spa_bookings+event_bookings), payroll_department_monthly, service_charge_monthly, labor_cost_monthly (ref ke revenue_runrate), payroll_access_level_monthly, business_line_group_monthly, property_benchmark_monthly (ref ke overall_monthly untuk ranking).
Result: Semua 9 tabel berhasil dibuild. `dbt test`: 19/19 PASS, 0 error. **Seluruh 6 domain + 2 kasus khusus kini lengkap: 49 fact table (Revenue 9, F&B 8, Facility/Ops 9, Spa & Event 6, HR 8, Corporate/Financial 9 -- naik dari target 45 di M5.2 karena pemecahan grain yang ditemukan saat implementasi) + 27 dimension table, seluruhnya tervalidasi terhadap skema BigQuery aktual (bukan asumsi di atas kertas).**

## 2026-08-08 — Checkpoint 8: DQ Gate + Promote + Validasi (Task 9-11)
Did:
- **Task 9**: Full `dbt run`+`dbt test` lintas seluruh 76 model (27 dim + 49 fact) sekaligus -- 76/76 build, 175/175 test PASS. Tulis singular test `warehouse/tests/assert_gop_no_double_counting.sql` (Keputusan #5). Uji coba terkontrol: cek `financial_summary` langsung ke BigQuery -- `gop` cuma non-zero di baris `Overall`/`Corporate Overhead` (0 di Room/F&B/Spa&Event), jadi bug `SUM(gop)` tanpa filter department TIDAK mengubah hasil (data sudah "terlindungi" secara struktural). Tapi `departmental_revenue` terisi di SEMUA baris -- test diarahkan ke `gop_margin_pct` (rentan denominator terinflasi). Suntik bug (hapus filter `WHERE department IN (...)`, ganti jadi `SUM(...) GROUP BY`), rebuild, test **FAIL 180 baris** (terbukti gate menangkap). Revert ke versi benar, rebuild, test **PASS** lagi.
- **Task 10**: Dataset target `mart_aggregated` (bukan `_staging`) ternyata belum ada di BigQuery (baru `mart_aggregated_staging`) -- dibuat sekali via `client.create_dataset()` (setara provisioning `mart_cleaned` dulu di M2.3, tidak terdokumentasi eksplisit tapi pola sama). Buat `scripts/mart_aggregated/promote.py` (copy persis pola `scripts/mart_cleaned/promote.py`, `STAGING_DATASET`/`TARGET_DATASET` diganti). Jalankan: `dbt run` 76/76 OK, `dbt test` 176/176 PASS, promosi `CREATE OR REPLACE TABLE` 76/76 tabel berhasil ke `mart_aggregated`.
- **Task 11**: Validasi manual 6 metrik representatif (1/domain) -- Revenue (occupancy_rate P01/Deluxe/2024-03-15: 0.4762=0.4762), F&B (total revenue: Rp98.370.508.260 = Rp98.370.508.260), Facility/Ops (total new_ticket_count: 13.514=13.514), Spa & Event (total revenue: Rp76.728.937.061=Rp76.728.937.061), HR (total present_count: 531.751=531.751), Corporate/Financial (total gop: Rp330.502.531.389=Rp330.502.531.389) -- **6/6 cocok persis**. Re-verifikasi Audit PII: query `INFORMATION_SCHEMA.COLUMNS` lintas seluruh `mart_aggregated` untuk pola `email`/`phone`/`guest_id`/`full_name` -- hasil cuma 1 baris (`dim_employee.full_name`), persis sesuai kesimpulan Audit PII M5.2 (tidak ada kolom `guests_pii` yang bocor).
Result: KK#1 (seluruh tabel terisi & tervalidasi), KK#2 (DQ gate terbukti menangkap pelanggaran via uji coba terkontrol), dan KK#3 (PII termask/diteruskan sesuai keputusan sadar, terverifikasi ulang di hasil akhir) ketiganya terpenuhi dengan bukti konkret.

## 2026-08-08 — Checkpoint 9: Dokumentasi Pendamping (Task 12-14)
Did:
- **Task 12**: `git mv docs/07-mart-aggregated/desain-skema-mart-aggregated.md` → `DataSchema-mart-aggregated.md` (Keputusan #7). Update seluruh 6 referensi nama lama (`docs/keputusan-tertunda.md`, `milestones/5.2-.../{decisions,logs,report}.md`, `milestones/5.3-.../{decisions,logs}.md`) via bulk sed replace, koreksi 1 kalimat yang jadi rusak akibat replace ganda ("X di-rename jadi X" → dikembalikan ke nama lama di sisi kiri kalimat).
- **Task 13**: Tulis `Metadata-mart-aggregated.md` (Keputusan #8) — konteks, per-domain cara hitung tiap fact table (bukan cuma nama kolom), 3 catatan penting Corporate/Financial (disambiguasi department, aturan Overall/Corporate Overhead, gap breakdown undistributed_expense), Audit PII re-verifikasi, ringkasan Validasi Silang Task 11.
- **Task 14**: Tulis `ERD-mart-aggregated.md` — 1 diagram Mermaid `erDiagram` tunggal, 76 entitas (27 dim + 49 fact) + ~90 relasi (Keputusan #9, user pilih 1 diagram komprehensif bukan split per domain). Verifikasi jumlah entitas per domain cocok dengan hitungan fact table final (Revenue 9, F&B 8, Facility/Ops 9, Spa & Event 6, HR 8, Corporate/Financial 9 = 49; dimension 27).
Result: 3 file dokumentasi pendamping selesai. Tidak ada referensi tersisa ke nama file lama (`grep -rl desain-skema-mart-aggregated` kosong).
