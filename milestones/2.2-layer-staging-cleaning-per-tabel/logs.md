# Milestone 2.2 -- Execution Log

## 2026-08-08 (start) -- Perubahan alur kerja untuk milestone ini
Did: User secara eksplisit meminta Milestone 2.2 TIDAK dimulai dari breakdown+decisions seperti M2.0/M2.1 -- melainkan dimulai dari **observability/profiling data** independen terhadap 23 tabel production, sebelum keputusan cleaning per tabel diambil. Alasan user: `docs/01-architecture/Metadata.md` sudah mendokumentasikan pola dirty-by-design, tapi sebagai data engineer, verifikasi langsung ke data tetap wajib dilakukan untuk menangkap hal yang mungkin tidak tercatat di dokumen panduan. Hasil profiling kemungkinan akan jadi dokumen tambahan di luar 3 file standar (`decisions.md`/`logs.md`/`report.md`), dicatat progresif di log ini selama berjalan.
Result: n/a -- catatan konteks, bukan hasil kerja.

## 2026-08-08 -- Setup profiling
Did: Baca ulang `docs/01-architecture/Metadata.md` untuk daftar pola dirty-by-design yang SUDAH terdokumentasi (baseline pembanding) -- ditandai emoji "🧹": `employees.full_name` (whitespace ~2%), `employees.role_title` (~2% kosong), `employees.department` (19 variasi tulisan untuk 8 departemen -- "PALING KOTOR"), `employees.hire_date` (~2% format DD/MM/YYYY), `guests.full_name` (~2% typo), `guests.email` (~4% kosong), `guests.phone` (4 variasi format + ~3% kosong), `guests.nationality` (kapitalisasi tidak konsisten), `guests` 367 baris duplikat. Plus null-bermakna yang didokumentasikan di tempat lain: `fnb_transactions.guest_id` (~31% walk-in), `spa_bookings.guest_id` (~21% walk-in), `maintenance_tickets.room_id`/`parts_replaced`/`resolved_date`, `staff_shifts.clock_in`/`clock_out` (absent/leave), `employee_performance.score` (tenure <3bln), `financial_summary.gop`/`undistributed_expense` (hanya baris 'Overall'), `payroll.thr` (hanya Maret), `fnb_waste_log` (~25% cakupan sampel by design).

## 2026-08-08 -- Bangun & jalankan `scripts/profiling/profile_tables.py`
Did: Tulis script profiling generik (manual-only, pola sama seperti `scripts/schema_drift/baseline_columns.py`) -- per kolom: null count/pct, distinct count, deteksi whitespace & case-variant untuk teks, min/max/negative count untuk angka, plus 1 pemeriksaan duplikat full-row per tabel. Pakai `EXTRACT_DB_URL` (`extract_reader`, sudah tepat whitelist 23 tabel dari M2.1) bukan koneksi admin -- least-privilege paling pas untuk kerja read-only ini.
Result: worked, 23/23 tabel berhasil di-profile, output `scripts/profiling/output/profile_20260808T013253Z.json` (42KB).

## 2026-08-08 -- Analisis hasil + verifikasi bertarget
Did: Baca seluruh hasil profiling, bandingkan manual ke `Metadata.md` kolom per kolom. Temuan awal yang butuh verifikasi lanjutan (generic check tidak cukup): (1) `guests` duplicate_row_groups=0 padahal dokumen bilang 367 duplikat -- karena PK (`guest_id`) ikut dibandingkan, pemeriksaan generik buta terhadap "orang sama, ID beda"; (2) `payroll.thr`/`financial_summary.gop`/`undistributed_expense` null_count=0 padahal dokumen menyiratkan "kosong" untuk baris tak berlaku. Jalankan query manual bertarget untuk keduanya.
Result: (1) **Dikonfirmasi** -- 368 baris `guest_id >= G24501` cocok `full_name` (case/trim-insensitive) dengan guest ber-ID lebih awal, mendekati persis klaim 367 (selisih 1, kemungkinan boundary). Kunci pencocokan ternyata `full_name` SAJA, bukan kombinasi phone/email (yang sengaja dibuat beda untuk simulasi re-entry). (2) **Ditemukan koreksi dokumentasi**: "kosong" ternyata berarti **nilai 0**, bukan NULL SQL -- dibuktikan lewat `GROUP BY` bulan (payroll) dan departemen (financial_summary). Ekstra: `financial_summary` ternyata juga punya angka P&L nyata di `department='Corporate Overhead'` (bukan cuma `'Overall'` seperti tertulis di dokumen) -- khusus untuk P06 (kantor pusat, tidak punya departemen operasional biasa). Temuan tambahan (bukan dari rencana awal, ditemukan saat baca hasil JSON): `nationality` jauh lebih kotor dari "~3%" (466 distinct, 156 case-variant group), dan `fnb_transactions.transaction_id` ternyata tidak unik (387.972 distinct / 902.574 baris, ~2.33 baris per transaksi -- konsisten multi-item per order, tidak disebutkan eksplisit di `Metadata.md`).
Pelajaran: pemeriksaan otomatis generik (null/distinct/duplicate full-row) menangkap sebagian besar pola, tapi 2 temuan paling berharga (guests duplicate, payroll/financial_summary zero-vs-null) baru ketahuan lewat query manual bertarget setelah melihat hasil awal terlihat "aneh" (0 di tempat yang seharusnya ada masalah) -- tidak cukup berhenti di angka pertama tanpa curiga kalau tidak cocok ekspektasi.

## 2026-08-08 -- Dokumen temuan
Did: Tulis `milestones/2.2-layer-staging-cleaning-per-tabel/data-profiling-findings.md` (dokumen ke-4 di luar 3 file standar milestone, sesuai arahan user) -- struktur: konfirmasi vs dokumentasi, temuan baru/koreksi, catatan tambahan, keterbatasan metodologi, dan daftar item yang perlu keputusan eksplisit di `decisions.md` nanti.
Result: worked. Dokumen ini jadi input untuk breakdown+decisions Milestone 2.2 berikutnya, bukan pengganti `Metadata.md`.

## 2026-08-08 -- Kategorisasi solusi per temuan (sebelum breakdown task)
Did: User minta setiap temuan di `data-profiling-findings.md` dikategorikan: dibersihkan di staging vs dibiarkan untuk Data Scientist. Saat menyusun kategorisasi, ditemukan **kontradiksi eksplisit** di `docs/03-implementation-plans/02-serving-data-scientist.md` sendiri: baris 88 (lingkup M2.2) minta normalisasi format telepon, tapi baris 30 (Prinsip Kunci) menyebut "format telepon tidak konsisten" sebagai contoh dirty data yang sengaja dipertahankan untuk DS. Tidak menebak salah satu -- diajukan ke user via `AskUserQuestion`.
Result: User pilih **bersihkan** (ikuti lingkup M2.2 literal, bukan Prinsip Kunci). Dicatat sebagai deviasi sadar terhadap Prinsip Kunci, bukan oversight -- akan direferensikan lagi saat `decisions.md` M2.2 ditulis. Kategorisasi lengkap (Kategori A: dibersihkan, B: dibiarkan, C: bukan soal kotor/bersih tapi butuh keputusan dokumentasi/struktur) ditambahkan sebagai Section 6 di `data-profiling-findings.md`.

## 2026-08-08 -- Breakdown formal via skill + baca pemetaan-kebutuhan-konsumen-data-mart.md
Did: Panggil skill `planning-and-task-breakdown` (kedua kalinya di sesi ini, sesuai permintaan eksplisit user). Baca `docs/02-requirements/pemetaan-kebutuhan-konsumen-data-mart.md` -- dokumen rujukan RESMI M2.2 yang ternyata sudah berisi aturan cleaning spesifik per tabel/kolom. Temuan penting: dokumen ini MENGKONFIRMASI (bukan bertentangan) normalisasi `guests.phone` -- klarifikasi kontradiksi yang sempat ditemukan sebelumnya (vs Prinsip Kunci generic). 4 keputusan diajukan via `AskUserQuestion`.
Result: User pilih dbt-core (tooling), view (materialisasi), decisions.md-saja (dokumentasi Kategori C, tidak update dokumen master), case/whitespace saja (nationality). `decisions.md` M2.2 ditulis lengkap.

## 2026-08-08 -- Setup dbt-core (Task 1) + model percobaan (Task 2)
Did: Install `dbt-core`+`dbt-bigquery`. Buat folder `warehouse/` (dbt project baru, terpisah dari `scripts/`). Buat dataset `staging` di BigQuery. `dbt debug` pertama gagal -- `extract-writer` (scope dataset-only dari M2.1) tidak punya izin `bigquery.datasets.create` yang dibutuhkan dbt-bigquery meski dataset sudah ada. Buat service account baru `dbt-transform`, tapi 2x percobaan grant IAM role project-level diblokir classifier keamanan sesi -- diserahkan ke user untuk dijalankan manual (role grant + key file), sama seperti pola key file M2.1.
Result: setelah user konfirmasi selesai, `dbt debug` sukses. Model percobaan `stg_corporate_master__properties`/`role_permissions` (passthrough) berhasil `dbt run`. Bug ditemukan & diperbaiki: `+schema: staging` di `dbt_project.yml` menghasilkan dataset salah `staging_staging` (perilaku default dbt: schema custom digabung ke target schema, bukan menggantikan) -- dihapus confignya, cukup andalkan `dataset: staging` di `profiles.yml`, dataset salah dihapus (`bq rm -r`). Verifikasi ulang: row count staging = raw untuk kedua tabel (6=6, 77=77).

## Status saat ini
Task 1-2 selesai & terverifikasi. Lanjut Task 3-5 (mapping department, model employees & guests -- tabel paling kompleks, checkpoint milestone ini).
