# Milestone 1.6 -- Execution Log

## 2026-08-07 (start)
Did: Breakdown Milestone 1.6+1.7 sekaligus lewat `planning-and-task-breakdown` (routing `using-agent-skills`), konfirmasi 4 keputusan awal (stack API, data scope, proteksi, stack web) + 1 keputusan susulan (repo API terpisah, simetris dengan repo web) via `AskUserQuestion`. Tulis `decisions.md`. Update `.gitignore` root untuk `api/` dan `web/` (dikerjakan lokal, di-deploy dari repo terpisah).
Result: worked.

## 2026-08-07 -- Whitelist sample data production (Task 3)
Did: Baca `docs/01-architecture/Metadata.md` untuk 3 kandidat tabel (`properties`, `fnb_outlets`, `rooms`) -- verifikasi tidak ada kolom PII/finansial/HR di ketiganya. Finalisasi whitelist, dokumentasikan di `decisions.md` termasuk daftar eksplisit tabel yang TIDAK masuk whitelist beserta alasannya.
Result: worked. `properties` (6 baris), `fnb_outlets` (17 baris), `rooms` (549 baris) -- semua master data statis/operasional, tidak terhubung ke identitas tamu.

## 2026-08-07 -- Role Postgres read-only untuk API (Task 2)
Did: `scripts/api_reader/grants.sql` (GRANT SELECT scoped ke schema `monitoring` penuh + `ALTER DEFAULT PRIVILEGES` untuk tabel monitoring baru di masa depan, plus 3 tabel whitelist) dan `scripts/api_reader/setup_reader_role.py` (create/rotate role `monitoring_api_reader` dengan password random `secrets.token_urlsafe`, NOSUPERUSER/NOCREATEDB/NOCREATEROLE, apply grants, verifikasi otomatis: bisa SELECT whitelist, ditolak SELECT tabel non-whitelist, ditolak INSERT ke `monitoring.alerts`, tulis connection string ke `api/.env` sebagai `API_DB_URL` tanpa pernah print password penuh ke terminal).
Result: worked di percobaan kedua. Error pertama: `psycopg2.OperationalError: ... FATAL: (ENOIDENTIFIER) no tenant identifier provided` -- root cause: Supabase pooler (Supavisor) multi-tenant, username login wajib format `<role>.<project_ref>`, bukan cuma nama role polos (pola yang sama dengan `SUPABASE_DB_URL` yang sudah ada, `postgres.<project_ref>`, tapi belum disadari perlu direplikasi untuk role baru). Fix: derive `project_ref` dari `SUPABASE_DB_URL` yang ada, susun `pooler_user = f"{ROLE}.{project_ref}"`. Setelah fix, 4/4 verifikasi otomatis lulus.

## 2026-08-07 -- FastAPI skeleton + seluruh endpoint monitoring + sample data + rate limit/CORS (Task 1, 4, 5, 6)
Did: `api/app/db.py` (koneksi read-only pakai `API_DB_URL` dari role `monitoring_api_reader`), `api/app/queries.py` (SQL untuk tiap endpoint -- sengaja disalin persis dari query panel `scripts/grafana/build_dashboard.py` yang sudah tervalidasi, termasuk filter `_simulation`/`is_simulated`, supaya API dan dashboard Grafana tidak pernah berbeda jawaban), `api/app/main.py` (FastAPI app, 8 endpoint: `/health`, `/api/status/tables`, `/api/dq/summary`, `/api/dq/failures`, `/api/dq/dirty-proportion`, `/api/dq/anomalies`, `/api/schema-drift`, `/api/alerts`, `/api/sample/{table}`), rate limiting via `slowapi` (default 60/menit per IP, `/health` dikecualikan), CORS via `CORSMiddleware` (origin dari env `CORS_ALLOW_ORIGINS`, default `*` sampai domain web/ final ada).
Setelah Task 1-6 selesai: `git init` folder `api/`, commit, `gh repo create Ardiyanto24/nirwana-monitoring-api --public --push` -- repo publik baru berhasil dibuat & di-push (https://github.com/Ardiyanto24/nirwana-monitoring-api), `render.yaml` disiapkan untuk Render Blueprint. Diminta konfirmasi user soal platform deploy (Railway vs Render) & visibilitas repo lewat `AskUserQuestion` -- user pilih Render + publik.

## 2026-08-07 -- Koreksi proses: milestone tidak pernah ditulis di source doc
User menegur: Milestone 1.6 & 1.7 langsung dieksekusi tanpa lebih dulu ditulis sebagai rancangan resmi di `docs/03-implementation-plans/01-monitoring-data-production-fase1.md` -- pelanggaran konvensi `milestone-execution` sendiri (Phase 1 seharusnya mulai dari "baca source doc", bukan langsung `decisions.md` untuk milestone yang source doc-nya belum ada). Perbaikan: tambahkan section Milestone 1.6 & 1.7 ke source doc (dengan catatan eksplisit bahwa ini penambahan 2026-08-07, di luar rancangan Fase 1 semula, plus alasan kenapa), perbaiki `Source:` di `decisions.md` M1.6 untuk merujuk ke situ, tulis `report.md` sekarang (status In Progress, bukan Completed, karena Task 7 belum selesai) alih-alih menunggu deploy selesai -- supaya jejak dokumentasi tetap akurat terhadap urutan kejadian yang sebenarnya, bukan direkonstruksi seolah prosesnya benar dari awal.
Result: worked -- dicatat sebagai penyimpangan proses jujur, bukan disembunyikan. Pelajaran untuk milestone berikutnya: baca/tulis source doc SEBELUM `decisions.md`, bahkan untuk milestone yang scope-nya lahir dari diskusi ad-hoc (bukan cuma milestone yang sudah direncanakan dari awal proyek).

## 2026-08-07 -- Verifikasi endpoint (rangkuman, dari entri sebelumnya)
Result: worked. Dijalankan lokal (`uvicorn`), diuji tiap endpoint via curl: `/api/status/tables` 23 baris (cocok M1.2), `/api/dq/summary` 23 baris, `/api/dq/failures` 3 baris, `/api/dq/dirty-proportion` 8 baris, `/api/dq/anomalies` 6 baris, `/api/schema-drift` 0 baris, `/api/alerts` 28 baris -- semua konsisten dengan panel Grafana yang sudah ada. `/api/sample/properties` & `/api/sample/rooms` sukses (whitelist), `/api/sample/guests` (bukan whitelist) -> 404. Uji coba terkontrol rate limit: restart server dengan `RATE_LIMIT=3/minute`, request ke-4 & ke-5 -> 429 (server pertama sempat tidak mati bersih di Windows -- proses lama masih pegang port 8123, ketahuan dari `netstat`, di-`taskkill` manual sebelum uji ulang di port baru). CORS header `access-control-allow-origin: *` terkonfirmasi lewat curl `-I` dengan header `Origin` custom.

## 2026-08-07 -- Deploy publik ke Render (Task 7), dituntaskan
Did: User setup akun Render sendiri, hubungkan repo `nirwana-monitoring-api`. Beberapa hambatan berurutan, diselesaikan bareng lewat chat:
1. **Repo tidak muncul di pemilihan Render** -- GitHub App Render belum diberi akses ke repo baru (dibuat setelah instalasi app). Fix: user tambahkan repo lewat github.com/settings/installations > Render > Repository access.
2. **Start Command salah** -- Render tampilkan placeholder default `gunicorn your_application.wsgi` (untuk Flask), bukan `render.yaml` yang ter-deteksi otomatis (user pakai form manual, bukan Blueprint). Fix: isi manual `uvicorn app.main:app --host 0.0.0.0 --port $PORT`.
3. **Env var `API_DB_URL` lupa diisi sama sekali** di percobaan pertama -- endpoint data 500, `/health` tetap 200 (jelas indikasi bukan crash total, tapi masalah koneksi DB).
4. **Env var terisi tapi masih 500 setelah diisi** -- cek `Logs` tab bareng user: `psycopg2.OperationalError: ... FATAL: database "postgres<sesuatu>" does not exist`. Root cause: newline/spasi tambahan ikut ter-copy ke akhir value saat paste dari file `.env` (file text lazim diakhiri newline) -- nama database jadi rusak karena itu komponen terakhir connection string. Fix: user hapus whitespace di ujung value field Render, redeploy manual.
Result: worked setelah fix ke-4. Verifikasi publik penuh dari luar (curl, bukan localhost) ke `https://nirwana-monitoring-api.onrender.com`: seluruh 9 endpoint (`/health` + 7 endpoint data + `/api/sample/{table}`) mengembalikan status code & row count yang benar, whitelist sample data terisolasi (`properties`/`rooms` 200, `guests` 404), CORS header `access-control-allow-origin: *` terkonfirmasi. Task 7 selesai -- Milestone 1.6 tuntas seluruh Task Breakdown.
Pelajaran: env var yang di-copy-paste dari file text lokal ke platform deploy pihak ketiga rawan membawa whitespace tersembunyi (newline akhir baris) -- perlu dicek eksplisit di sisi form tujuan, bukan diasumsikan aman hanya karena sumbernya sudah benar.
