# Milestone 5.2 — Execution Log

## 2026-08-08 — Checkpoint 1: Fondasi Dimensional Model (Task 1)
Did: Baca ulang 94 baris metrik + kolom "Dimensi (filter & group-by)" di `pemetaan-kebutuhan-data-analyst.md` per domain untuk inventarisasi dimension table. Bangun 23 dimension table (star schema, sesuai Keputusan #1-2): 4 lintas-domain (dim_property, dim_employee, dim_department, dim_customer_type), 5 Revenue (dim_room_type, dim_room, dim_channel, dim_loyalty_tier, dim_nationality_group), 4 F&B (dim_outlet, dim_outlet_type, dim_fnb_category, dim_menu_item), 3 Facility/Ops (dim_facility_area, dim_issue_type, dim_priority), 4 Spa & Event (dim_spa_service, dim_venue, dim_venue_type, dim_event_type), 1 HR (dim_shift_type), 2 Corporate/Financial (dim_business_line, dim_access_level). Buat file `docs/07-mart-aggregated/desain-skema-mart-aggregated.md` dengan skeleton (Dimension Tables terisi penuh, Fact Tables + Audit PII placeholder).
Result: Worked. Temuan penting saat inventarisasi: `dim_business_line` (Corporate/Financial, taksonomi USALI Room/F&B/Spa&Event/Overall/Corporate Overhead) sengaja dipisah dari `dim_department` (HR, unit organisasi karyawan) meski dokumen sumber sama-sama menyebutnya "department" — keduanya set nilai berbeda, disambiguasi didokumentasikan eksplisit di desain agar tidak salah query di M5.3. `dim_room` dan `dim_customer_type` dicatat dipakai lintas domain (Revenue+Facility/Ops; F&B+Spa).

## 2026-08-08 — Checkpoint 2: Fact Table Revenue + F&B (Task 2-3)
Did: Desain 5 fact table Revenue (`fact_revenue_daily`, `fact_revenue_pricing_deviation`, `fact_revenue_property_summary`, `fact_revenue_loyalty_daily`, `fact_revenue_nationality_daily`) dan 8 fact table F&B (`fact_fnb_outlet_daily`, `fact_fnb_category_daily`, `fact_fnb_hourly`, `fact_fnb_customer_type_daily`, `fact_fnb_menu_item_daily`, `fact_fnb_waste_daily`, `fact_fnb_inventory_status`, `fact_fnb_ingredient_price_daily`). Tambahkan 3 dimension table yang terlewat di Task 1 (`dim_pricing_reason`, `dim_waste_reason`, `dim_ingredient`) sebagai amendemen terdokumentasi, bukan revisi diam-diam.
Result: Worked. 1 temuan gap M5.1: metrik "booking per loyalty_tier" tersirat sebagai dimensi di §1.3 tapi tidak eksplisit jadi baris metrik konsolidasi — ditambahkan sebagai `fact_revenue_loyalty_daily`, dicatat eksplisit sebagai penambahan bukan revisi dokumen M5.1 yang sudah closed. Seluruh 15 baris Revenue dan 15 baris Cakupan Awal/Khusus F&B (dari 17, 2 Luar Cakupan tidak perlu tabel) tercakup, termasuk 1 baris (daftar staf F&B) yang sengaja TIDAK dapat fact table karena cukup dilayani query dimension langsung.

## 2026-08-08 — Checkpoint 3: Fact Table Facility/Ops + Spa & Event (Task 4-5)
Did: Desain 9 fact table Facility/Ops (room status, housekeeping room-type/property/staff, maintenance ticket/cost/room-recurrence/property-benchmark/technician) dan 6 fact table Spa & Event (spa daily/customer-type/service, event venue/property/type).
Result: Worked. Seluruh 14 baris Cakupan Awal Facility/Ops tercakup (3 Luar Cakupan tidak perlu tabel); kolom SLA disimpan mentah (`avg_sla_duration_hours`) tanpa flag breach sesuai Keputusan #7. Seluruh 13 baris Cakupan Awal Spa & Event (7 Spa + 6 Event/MICE) tercakup. 2 metrik per domain sengaja tidak dapat tabel terpisah karena diturunkan via roll-up dari tabel lain (tren maintenance jangka panjang dari `fact_maintenance_cost_daily`; distribusi status kamar per properti dari `fact_facility_room_status_daily`).

## 2026-08-08 — Checkpoint 4: Fact Table HR + Corporate/Financial (Task 6-7)
Did: Desain 6 fact table HR (attendance, employee monthly, turnover, headcount status, performance per departemen, performance per status) dan 9 fact table Corporate/Financial (business line monthly, overall/GOP monthly, revenue run-rate, payroll per departemen, service charge, labor cost, payroll per access level, business line grup, property benchmark). Tambahkan 1 dimension table yang terlewat (`dim_employee_status`).
Result: Worked. Watchlist HR (baris 5 M5.1) sengaja TIDAK didesain di sini, dipindah ke Task 8 sesuai Keputusan #9. Seluruh 9 baris Cakupan Awal HR (dari 10, 1 dipindah ke Task 8) tercakup. Temuan disambiguasi penting: kolom "department" di `payroll` (dipakai `fact_payroll_*`) merujuk unit organisasi karyawan (`dim_department`, sama seperti HR) — BUKAN `dim_business_line` (USALI) yang dipakai tabel `fact_financial_*` dari `financial_summary` — didokumentasikan eksplisit agar tidak tertukar saat implementasi M5.3. Seluruh 13 baris Cakupan Awal Corporate/Financial tercakup, termasuk "Overhead Korporat" yang ternyata tidak butuh tabel terpisah (cukup jadi salah satu nilai `dim_business_line`).

## 2026-08-08 — Checkpoint 5: Kasus Khusus Lintas Domain (Task 8)
Did: Desain `fact_revenue_pace_booking_snapshot` (grain property/room_type/stay_date/snapshot_date, partition by snapshot_date) dan `fact_hr_watchlist_monthly` (grain employee/periode, kolom rasio deviasi mentah tanpa flag). Fix duplikat heading "## Audit PII" yang tidak sengaja tertulis 2x saat edit sebelumnya.
Result: Worked. Ditemukan 1 tension penting untuk M5.3: `fact_revenue_pace_booking_snapshot` butuh append-only (baris snapshot lama tidak pernah diupdate) tapi seluruh tabel lain di skema ini + `mart_cleaned` full-refresh mengikuti constraint BigQuery Sandbox mode (DML diblokir total) — dicatat eksplisit sebagai pertanyaan implementasi untuk M5.3, bukan diselesaikan di M5.2 (scope M5.2 cuma menjamin struktur benar, bukan cara operasionalnya di Sandbox mode). `fact_hr_watchlist_monthly` konsisten Keputusan #7 -- tidak ada kolom flag watchlist karena threshold belum ditentukan.

## 2026-08-08 — Checkpoint 6 (final): Audit PII + Finalisasi (Task 9-10)
Did: Telusuri seluruh 45 fact table + 27 dimension table untuk kolom yang berpotensi PII (`guests_pii`, `guests_profile`, `employees_directory`). Tulis bagian "Audit PII" dengan keputusan eksplisit per kolom + konfirmasi tidak ada kolom `guests_pii` mentah di skema manapun. Perbaiki duplikat heading dan koreksi hitungan (23->27 dimension table setelah amendemen, 45 fact table). Update status header dokumen ke Selesai. Verifikasi 3 KK sumber, tulis `report.md`. Update `decisions.md` status Planned->Done.
Result: Worked. Audit PII menyimpulkan TIDAK ADA kolom yang perlu masking -- desain skema (star schema teragregasi, tanpa grain per-tamu) secara struktural sudah tidak pernah memuat guest PII mentah sejak awal. 3 kolom personal teridentifikasi (full_name, loyalty_tier_name, nationality group_name) semuanya diputuskan "diteruskan apa adanya" dengan alasan eksplisit. Ketiga KK sumber terpenuhi, dicek dengan bukti konkret di `report.md`. Milestone 5.2 ditutup Completed.
