# Milestone 6.3 — Execution Log

## 2026-08-11 — Plan mode: breakdown, 3 Explore agent paralel
Did: `/planning-and-task-breakdown` dipanggil. 3 Explore agent paralel: (1) internals `promote.py` kedua versi + viabilitas `run_results.json`, (2) pola rolling-baseline Fase 1 (`detect_alerts.py`/`snapshot_value_anomaly.py`/`dq_alerts.py`), (3) sinyal `ml_output`/row-count-parity yang sudah ada. Temuan kunci: `run_results.json` tidak terekspos API manapun (beda dari M6.2), test count mart_aggregated ternyata 190 bukan 244+1 (drift dari M5.3), `reverse_etl_sync_log` sudah lengkap untuk Output 3 (0 mekanisme baru), staging = view (dikecualikan dari volume anomaly). Plan ditulis lengkap (15 keputusan teknis, semua dikunci dengan reasoning — tidak ada `AskUserQuestion`, beda dari M6.2 yang 3 fork genuinely seimbang) dan disetujui user tanpa revisi.

## 2026-08-11 — Checkpoint 1: kredensial `warehouse-monitor-reader`
Did: `gcloud iam service-accounts create warehouse-monitor-reader`. Grant dataset ACL READER ke 4 dataset (`raw_production`,`mart_cleaned`,`mart_aggregated`,`ml_output`) via `bq show`/`bq update --source` (workaround `bq add-iam-policy-binding` "requires allowlisting", precedent M2.1/M3.6).
Result: **2 hambatan teknis ditemukan & diperbaiki** (dicatat detail di `decisions.md` Keputusan #12): (1) kombinasi `bq.exe` + Python baca/tulis JSON via path Git-Bash menghasilkan file kosong/tidak ditemukan secara silent (persis gejala precedent M2.1 "native Windows Python tidak mengerti /tmp Git Bash") — pindah ke PowerShell; (2) `Out-File -Encoding utf8` PowerShell 5.1 menambah BOM yang bikin parser JSON `bq update` gagal ("Expecting value: line 1 column 1") — diperbaiki pakai `[System.IO.File]::WriteAllText` + `UTF8Encoding($false)` eksplisit. Setelah diperbaiki: 4/4 dataset ter-update sukses. `roles/bigquery.jobUser` project-level ditambahkan. Key file dibuat sukses (tidak diblokir classifier sesi ini), dikonfirmasi gitignored.
Verifikasi: `verify_dataset_isolation.py` 5/5 OK (4 allow + 1 deny `staging`), percobaan `CREATE TABLE` ditolak `403 Forbidden`. `.env`/`.env.example`/`kebijakan-akses-kredensial-scoped.md` diperbarui.

## 2026-08-11 — Checkpoint 1: extend schema.sql
Did: Append `dbt_test_result`, `warehouse_volume_snapshot`, `ml_output_freshness_snapshot`, view `warehouse_parity_status`, `ALTER reverse_etl_sync_log ADD is_simulated`, `ALTER monitoring.alerts` extend `alert_type` (4 nilai baru) ke `scripts/monitoring_warehouse/schema.sql`. `apply_schema.py` dijalankan sukses.
Result: worked. Seluruh objek dikonfirmasi live via `information_schema`/`pg_constraint`. `warehouse_parity_status` diuji langsung terhadap data `reverse_etl_sync_log` sungguhan (baris `mart_aggregated` real, `bq_row_count=pg_row_count`, `status='synced'`) — Keputusan #7 (murni konsolidasi) terbukti benar sejak awal. Commit `ef6c1a2`.

## 2026-08-11 — Checkpoint 2: capture_dbt_test_results.py
Did: Tulis script, uji manual terhadap `run_results.json` nyata yang sudah ada di disk (7 hasil test dari sesi sebelumnya) — parsing `test_name` dari `unique_id` dbt (`test.<project>.<name>.<hash>`) diverifikasi benar untuk 7/7 baris.
Result: worked. Ditambah step baru `if: always()` ke `transform-mart-cleaned.yml` (1 step) dan `transform-mart-aggregated.yml` (2 step terpisah — gate 76 tabel wajib + gate `tag:ml_feedback_loop` best-effort, masing-masing capture sendiri supaya tidak salah atribusi hasil). Kedua YAML divalidasi `yaml.safe_load` OK. Commit `8dca488`.

## 2026-08-11 — Checkpoint 2: Uji coba terkontrol KK1 (fault-injection nyata)
Did: User dikonfirmasi (`AskUserQuestion`) sebelum push — disetujui. Push, lalu edit sementara `mart_cleaned__bookings.sql` (`UNION ALL` 1 baris `total_amount=-500000`, pola persis M2.3), commit+push, trigger `transform-mart-cleaned.yml` manual (run `31436680183`).
Result: **worked, KK1 terbukti penuh**. Gate FAIL sesuai desain (swap dibatalkan, perilaku lama tidak berubah — dikonfirmasi step "renew expiration" ikut ter-skip). Step capture baru **tetap jalan** meski step sebelumnya gagal (`if: always()` terbukti benar di kondisi nyata, bukan cuma teori). Query `dbt_test_result`: 37 baris tercatat (36 milik `mart_cleaned` + 1 `assert_gop_no_double_counting` yang cross-reference `mart_cleaned__financial_summary`), **tepat 1** `status='fail'` (`assert_bookings_total_amount_non_negative`, `failures=1`) — cocok persis 1 baris yang disuntik. `github_run_id=31436680183` tercatat benar (cross-reference ke `pipeline_run_log.run_id` M6.2, Keputusan #2, siap di-JOIN). Model direvert immediately setelah verifikasi (commit `d998b15`, push) — `mart_cleaned` (dataset asli, bukan staging) tidak pernah tersentuh baris buruk sama sekali karena gate mencegah swap, konsisten jaminan M2.3 yang tidak berubah.

## 2026-08-11 — Checkpoint 3: Output 2 (KK2), volume anomaly BigQuery
Did: Coba `INFORMATION_SCHEMA.TABLE_STORAGE` (rencana awal Keputusan #4) -- **Access Denied** baik dataset-qualified maupun region-qualified (kemungkinan restriksi Sandbox mode, project belum billing). Diganti `__TABLES__` (legacy metadata pseudo-table) -- bekerja, angka diverifikasi akurat (employees=755, guests=24893, cocok M1.1). Keputusan #4 direvisi di `decisions.md` dengan penjelasan lengkap.
Tulis `snapshot_warehouse_volume.py`, dijalankan manual: 123 tabel (23 raw_production + 23 mart_cleaned + 77 mart_aggregated -- termasuk 1 tabel ML provisional yang memang ada fisik di BigQuery meski tidak disync ke Postgres, M5.5). `SUM(row_count)` raw_production=mart_cleaned=2.534.072 (cocok "~2.53M rows" CLAUDE.md).
Tulis `detect_volume_anomaly.py`, replikasi persis algoritma `detect_alerts.py`. Dites terhadap data 1-hari nyata: seluruh tabel `insufficient_history` (benar, `MIN_HISTORY_POINTS=3` belum terpenuhi).
Uji coba terkontrol KK2: insert 3 minggu histori sintetis (day-of-week sama) + outlier 10x untuk 2 tabel beda dataset (`mart_cleaned.mart_cleaned__employees`, `mart_aggregated.dim_access_level`). Detector menghasilkan **2 alert `critical` terpisah**, masing-masing `dataset`/`table_name` benar (KK2 "bisa dibedakan dari tahap mana asalnya" terbukti). Data sintetis + alert dibersihkan, nilai asli dipulihkan.
