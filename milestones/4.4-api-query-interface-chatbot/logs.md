# Milestone 4.4 — Execution Log

## 2026-08-10 -- Mulai, breakdown via planning-and-task-breakdown skill
Did: Baca kontrak sumber (`05-serving-ai-chatbot.md` Milestone 4.4), preseden `milestones/3.4-multi-endpoint-api-analyst/decisions.md` dan `scripts/data_analyst_api/` (main.py, connections.py, whitelist_revenue.py) untuk pola FastAPI internal. Identifikasi beda struktural krusial: M3.4 pakai 1 koneksi admin (isolasi diserahkan M3.5, dipakai manusia langsung di luar API) -- API M4.4 justru harus JADI trust boundary sesungguhnya (tidak ada manusia pemegang kredensial M4.3 langsung, chatbot dinamis lewat 1 API). Ditemukan gap: tidak ada kredensial M4.3 yang bisa baca role_permissions untuk keperluan otorisasi internal API -- butuh kredensial ke-11 (chatbot_authz_reader), dibuat di M4.4 (pola sama M5.7 menambal gap M5.2).
Result: Plan (route /chatbot/{domain}/{view_name}, otorisasi wajib via role_permissions, eksekusi lewat kredensial domain M4.3, own_property resolve-jangan-percaya, 7 fase/7 checkpoint) ditulis ke plan file, disetujui user, plan mode exited. `decisions.md` ditulis lengkap.

## 2026-08-10 -- Fase 0: kredensial chatbot_authz_reader + fondasi API
Did: `role_config_authz.py` (GRANT SELECT ke mart_cleaned.role_permissions saja). Update `connections.py`/`setup_chatbot_roles.py` M4.3: kembalikan `get_mart_cleaned_owner_connection()` (sempat dihapus M4.3 karena tidak dibutuhkan saat itu) + `apply_grants()` diperluas merutekan schema mart_cleaned lewat REVERSE_ETL_WRITER_DB_URL (owner sebenarnya, admin silent no-op -- temuan M3.5), chatbot_views tetap lewat admin. `python setup_chatbot_roles.py --role chatbot_authz_reader`.
Bangun `scripts/chatbot_api/{connections.py,authz.py,main.py}`: connections.py (10 getter koneksi domain M4.3 + koneksi authz + resolve_property_id via v_employees_directory), authz.py (authorize() cek role_permissions sebelum query data apa pun), main.py (FastAPI, route generik /chatbot/{domain}/{view_name}, _run_whitelisted_query menangani override property_id untuk own_property). `/health` + uvicorn lokal.
Result: worked. chatbot_authz_reader: 6/6 check OK (termasuk GRANT via owner-routing mart_cleaned berhasil, beda dari 10 kredensial sebelumnya yang semua lewat admin/chatbot_views). Server FastAPI start normal, curl /health -> {"status":"ok"}.

## 2026-08-10 -- Fase 1: reservation + fnb -- Error & Fix
Encountered: `psycopg2.errors.UndefinedTable: relation "role_permissions" does not exist` saat test HTTP pertama -- `authz.py` query tanpa prefix schema, `chatbot_authz_reader` tidak punya default `search_path` ke `mart_cleaned`.
Cause: Query di `get_access_scope()` pakai `FROM role_permissions` (bare), bukan `FROM mart_cleaned.role_permissions`.
Fix: Tambah prefix schema eksplisit. Juga ditemukan port 8010 dipakai proses uvicorn lama dari Fase 0 yang tidak benar-benar mati (`kill %1` di Git Bash tidak efektif untuk proses uvicorn background) -- diselesaikan dengan `netstat`+`taskkill //F` manual, dipakai konsisten sepanjang sisa milestone.

## 2026-08-10 -- Fase 1: reservation + fnb (lanjutan setelah fix)
Did: `whitelist_reservation.py` (10 entry) + `whitelist_fnb.py` (11 entry), register route di `main.py`. Server nyata (`uvicorn`) + `curl` end-to-end: (1) Front Office Staff (E0071, P01) akses `room-type-daily` -- data P01 benar; (2) `bookings` lookup dengan `property_id=P02` diklaim di query string -- **hasil tetap 100% P01**, membuktikan override own_property bekerja nyata (bukan cuma di kode); (3) Front Office Staff coba domain `fnb` -- 403; (4) role_title tidak dikenal -- 403; (5) own_property tanpa `employee_id` -- 400. F&B Staff (E0041, P01): fnb-transactions lookup sukses, coba domain `reservation` -- 403, view tidak ada di whitelist -- 404.
Result: worked (setelah fix). Seluruh 8 skenario uji (2 domain x 4 kasus) sesuai ekspektasi.

## 2026-08-10 -- Fase 2: facility + spa_event -- gap ditemukan & diperbaiki
Encountered: Saat merancang filter own_property untuk `v_housekeeping_staff_daily`/`v_maintenance_technician_daily` (facility), ditemukan kedua view itu (M4.2) TIDAK mengekspos `property_id` sama sekali -- cuma `staff_id`/`assigned_staff_id`+`period_date` -- meski `dim_employee` yang sudah di-join di view itu punya kolomnya. Tanpa perbaikan, filter own_property tidak bisa diterapkan sama sekali di 2 view ini (gap keamanan nyata -- own_property role bisa melihat performa staff lintas properti).
Cause: Saat M4.2 menulis kedua view (pola langsung reuse dari analyst_views M3.2), kolom property_id memang tidak pernah diminta ditampilkan -- M3.2 tidak butuh karena isolasi row-level per situasi berbeda (GRANT langsung, bukan filter API).
Fix: Tambah `e.property_id` di akhir SELECT `views_facility.sql` (pola sama "append di akhir" M4.2/M3.2), re-apply. Diverifikasi 0 baris NULL property_id di kedua view.
Did: `whitelist_facility.py` (12 entry) + `whitelist_spa_event.py` (9 entry), register route. Tes HTTP: Housekeeping Staff (E0002, P01) -- `housekeeping-staff-daily` dengan `property_id=P05` diklaim -- hasil tetap P01 (fix terbukti bekerja); `rooms` lookup sukses; coba domain `spa_event` -- 403. Spa & Event Staff (E0157, P01) -- `spa-bookings` sukses; coba domain `facility` -- 403.
Result: worked setelah fix. Seluruh 6 skenario uji sesuai ekspektasi, termasuk pembuktian langsung fix property_id.

## 2026-08-10 -- Fase 3: hr + financial
Did: `whitelist_hr.py` (10 entry, tanpa payroll) + `whitelist_financial.py` (11 entry). Tes HTTP: HR Staff (E0084, P01) -- staff-shifts lookup sukses, coba domain financial -- 403 (segregasi payroll tertegakkan juga di layer API, bukan cuma DB M4.3). Finance Manager (E0452, P03) -- departmental-margin dengan property_id=P01 diklaim -- hasil 100% P03 (override bekerja lintas Fase, bukan cuma P01 seperti tes sebelumnya), business_line_name cuma berisi F&B/Room/Spa&Event (Overall/Corporate Overhead ter-exclude, business rule M3.1 #1 terbukti sampai lapisan API); payroll lookup sukses.
Result: worked. Seluruh skenario sesuai ekspektasi.

## 2026-08-10 -- Fase 4: 4 domain granular -- generalisasi own_property_column + temuan RBAC
Did: Sebelum menulis whitelist guests, disadari `_run_whitelisted_query` hardcode nama kolom `property_id` untuk override own_property -- `guests_contact_view`/`guests_profile_view` (M4.2) pakai `last_active_property_id`, bukan `property_id`. Generalisasi `main.py`: entry whitelist boleh punya `own_property_column` opsional (default `property_id`), dipakai konsisten di seluruh logic skip-filter dan override.
`whitelist_properties_ref.py`/`whitelist_employees_directory.py` (1 entry masing-masing). `whitelist_guests_pii.py`/`whitelist_guests_profile.py` (1 entry masing-masing, `own_property_column="last_active_property_id"`).
Tes HTTP: properties_ref dan employees_directory sukses. Front Office Staff -> guests_pii sukses, hasil 100% last_active_property_id=P01.
**Percobaan uji krusial pertama gagal sebagai TES (bukan gagal sebagai sistem)**: Revenue Manager (dipilih sebagai "role guests_profile-only") ternyata BERHASIL akses guests_pii (HTTP 200) -- diinvestigasi, ternyata bukan bug: query `role_permissions` membuktikan Revenue Manager memang legitimately punya KEDUA domain guests_pii DAN guests_profile (revisi RBAC M1, "Revenue Manager mendapat tambahan guests_pii agar superset Staff-nya"). Verifikasi lanjut: `SELECT ... EXCEPT ...` membuktikan **tidak ada satu pun dari 20 persona** yang punya guests_profile TANPA guests_pii -- jadi skenario "role profile-only ditolak akses pii" secara struktural tidak mungkin diuji dari data RBAC nyata manapun. Diperbaiki: uji ulang pakai HR Staff (tidak permitted keduanya) -- ditolak di kedua arah (403/403), membuktikan mekanisme deny tetap benar tanpa memerlukan skenario yang memang tidak eksis di RBAC.
Result: worked (setelah generalisasi kolom + koreksi pilihan role uji). Seluruh 6 skenario akhir sesuai ekspektasi, termasuk temuan RBAC yang didokumentasikan sebagai fakta sistem, bukan gap.

## 2026-08-10 -- Fase 5: own_property vs all_properties end-to-end
Did: Corporate Revenue Director (all_properties, reservation) -- query tanpa employee_id dan tanpa filter property_id (all_properties tidak mewajibkan employee_id sama sekali, sesuai desain). Filter property_id=P02 dipakai sebagai penyempitan opsional -- berhasil, membuktikan tidak ada pembatasan hardcoded. Limit 1000 tanpa filter awalnya cuma menampilkan P01 -- diinvestigasi, ternyata efek ORDER BY property_id + volume data P01 yang besar (bukan pembatasan), dikonfirmasi dengan query eksplisit property_id=P02 yang berhasil. Kontras diulang dengan Front Office Staff (own_property) mengklaim property_id=P04 -- tetap 100% P01.
Result: worked. Kontras own_property (selalu 1 properti, dipaksa) vs all_properties (bebas lintas properti, filter opsional) terbukti nyata lewat HTTP, bukan cuma dari baca kode.

## 2026-08-10 -- Fase 6: finalisasi
Did: Tes tambahan CEO (Korporat, 10 domain all_properties) -- financial departmental-margin sukses, percobaan akses role_permissions lewat endpoint manapun tetap 404 struktural (tidak pernah terdaftar di whitelist mana pun). Tulis `docs/09-serving-ai-chatbot/api-chatbot.md` (cara kerja, endpoint, parameter, mekanisme penolakan, penanda stabil vs berpotensi berubah). Update `kebijakan-akses-kredensial-scoped.md` (+1 baris chatbot_authz_reader + catatan Siapa Boleh Memegang). Verifikasi ulang KK1-KK3, tulis `report.md`.
Result: worked. Ketiga Kriteria Keberhasilan sumber terpenuhi dengan bukti eksplisit dari HTTP call nyata sepanjang milestone (bukan cuma baca kode).

## 2026-08-17 -- Gap dilaporkan tim AI Chatbot (Lapis 1) & diperbaiki: filter `employee_id` hilang di 7 dari 9 view performa individu
Encountered: AI Engineer (Lapis 1) melaporkan bahwa dari 9 view chatbot_views bergrain per-individu staf (1 baris = 1 staf tertentu), hanya `v_lookup_staff_shifts`/`v_lookup_employee_performance` (whitelist_hr.py) yang mendeklarasikan filter `employee_id` di `entry["filters"]`. 7 lainnya -- `v_hr_employee_monthly`, `v_hr_employee_performance_semester`, `v_hr_watchlist_monthly` (whitelist_hr.py), `v_housekeeping_staff_daily`, `v_maintenance_technician_daily`, `v_lookup_housekeeping_log`, `v_lookup_maintenance_tickets` (whitelist_facility.py) -- tidak. Diverifikasi langsung: `main.py` `_run_whitelisted_query` (baris ~71-77) iterasi `entry["filters"]`, bukan `query_params` -- jadi `employee_id` yang dikirim Lapis 1 di request memang diabaikan sepenuhnya untuk 7 view itu. `authz.py` dikonfirmasi tidak punya mekanisme lain (access_scope cuma granularity properti) yang bisa menambal ini.
Cause: Saat whitelist dibuat di Fase 3 (hr)/Fase 2 (facility) milestone ini, 7 view itu memakai constant filter list bersama (`_PROPERTY_PERIOD_FILTERS`/`_PROPERTY_REVIEW_PERIOD_FILTERS`) yang didesain untuk view agregat (tidak punya kolom individu) -- kolom individu di 7 view lookup/per-employee ini tidak pernah ditambahkan secara terpisah, luput dari cakupan Fase 4/5/6 (yang menguji own_property di level properti, bukan di level baris per-individu di dalam satu properti).
Fix: Verifikasi nama kolom individu tiap view langsung ke `scripts/chatbot_views/views_hr.sql`/`views_facility.sql` (bukan asumsi): `employee_id` untuk 3 view HR, `staff_id` untuk `v_housekeeping_staff_daily`/`v_lookup_housekeeping_log`, `assigned_staff_id` untuk `v_maintenance_technician_daily`/`v_lookup_maintenance_tickets`. 7 entry whitelist diberi filter list sendiri (bukan lagi share constant) dengan tambahan `{"param": "employee_id", "column": "<kolom_individu>", "op": "="}`, param tetap bernama `employee_id` di kedua sisi (konsisten dengan yang sudah dikirim Lapis 1 sejak M4.4/sesuai laporan). `_PROPERTY_PERIOD_FILTERS`/`_PROPERTY_REVIEW_PERIOD_FILTERS` tetap dipakai apa adanya oleh view agregat lain yang memang tidak punya kolom individu.
Result: worked. Syntax-check (`ast.parse`) kedua file lolos. Gap tervalidasi nyata sebelum fix (bukan salah baca laporan Lapis 1) lewat pembacaan langsung `main.py`/`authz.py`/whitelist/SQL view, bukan cuma dipercaya dari pesan.
