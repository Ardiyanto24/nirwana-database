# Milestone 6.7 — Execution Log

## 2026-08-11 — Breakdown + Checkpoint 1
Did: Breakdown via `/planning-and-task-breakdown` lewat Plan Mode. Riset live: peta M6.1 (`docs/10-monitoring-warehouse-serving/pemetaan-titik-pengamatan-pipeline.md`), `titik_config.py` (M6.2), skema `monitoring.pipeline_run_log`/`alerts` (13 alert_type sebelum M6.7), scan seluruh `detect_*.py`/`check_*.py` untuk pola INSERT `monitoring.alerts` (ketemu `dbt_test_failure` reserved-tapi-mati), Grafana existing (`build_dashboard.py`/`create_alerts.py` M1.5, 1 datasource admin), `api/`+`web/` (M1.6/1.7 -- cek endpoint existing, `grants.sql` M1.6 ternyata sudah `ALTER DEFAULT PRIVILEGES`, status deploy `web/` dari `logs.md` M1.7).
2 putaran `AskUserQuestion`: (1) mekanisme root-cause grouping -- user pilih "View SQL + label Grafana"; kanal notifikasi eksternal -- user pilih "Tetap tunda". (2) User menginterupsi rencana awal (Grafana-only) dengan pertanyaan soal dashboard publik -- riset tambahan (`api/app/queries.py`, `web/` git status, `grants.sql`) lalu `AskUserQuestion` cakupan publik -- user pilih "Perluas penuh ke publik juga". Plan ditulis ulang 2x (grouping mechanism awal, lalu ekspansi Bagian B publik), sempat ditolak sekali karena kurang atomic task per checkpoint -- diperbaiki jadi 38 task eksplisit di bawah 8 checkpoint. Plan disetujui.
`decisions.md` ditulis. `scripts/monitoring_warehouse/schema.sql` extend: tabel `monitoring.titik_dependency` (12 baris: 10 titik + edge dependency dari peta M6.1) + alert_type baru `pipeline_dependency_gap`.
Result: worked, dengan 1 error di tengah jalan (lihat di bawah).

## 2026-08-11 -- Error
Encountered: `apply_schema.py` gagal `psycopg2.errors.CheckViolation: check constraint "alerts_alert_type_check" ... violated by some row` saat mengeksekusi blok DROP+ADD CONSTRAINT `alerts_alert_type_check` yang SUDAH ADA di file ini sejak M6.3 (10 nilai, line ~125-131).
Cause: `monitoring.alerts_alert_type_check` adalah SATU constraint dipakai bersama oleh 4 file `schema.sql` berbeda (`monitoring_warehouse`, `chatbot_perf_monitor`, `serving_layer_monitor`, `dq`) -- tiap file cuma menyimpan salinan HISTORIS list nilai pada saat file itu ditulis, bukan referensi ke definisi tunggal. Blok M6.3 di file ini (10 nilai) sudah basi sejak M6.5 (`chatbot_connection_pool_spike`) dan M6.6 (`serving_swap_orphan_table`/`serving_swap_slow`) menambah nilai baru lewat file LAIN -- re-run file ini hari ini mencoba mengembalikan constraint ke versi basi 10-nilai, ditolak Postgres karena baris nyata sudah ada dengan alert_type di luar 10 nilai itu.
Fix: Hapus blok DROP+ADD CONSTRAINT basi (M6.3) di file ini, diganti catatan penjelasan; definisi final (union seluruh alert_type sampai M6.7, 14 nilai) dipindah ke SATU blok di akhir file (bagian Checkpoint 1 M6.7). Tidak menyentuh `chatbot_perf_monitor`/`serving_layer_monitor`/`dq` punya file sendiri (di luar scope M6.7 memperbaiki pola ini project-wide -- flagged, tidak diperbaiki total).
Verifikasi: `apply_schema.py` sukses re-run. Query manual `monitoring.titik_dependency`: 12 baris, cocok 1:1 dengan kolom Dependency+Klasifikasi Prioritas peta M6.1 (Titik 2 = kritis, Titik 4/5 = sedang, sisanya tinggi). `pg_constraint` konfirmasi `alerts_alert_type_check` ada.

## 2026-08-11 -- Checkpoint 2
Did: View `monitoring.titik_event_today` (union `pipeline_run_log` gagal + `monitoring.alerts` 13 alert_type di-map CASE alert_type+schema_name->titik_id + `monitoring.dbt_test_result` gagal). View `monitoring.alerts_with_root_cause` (recursive CTE naik `titik_dependency`, hasilkan `root_titik_id`+`is_root`). `scripts/monitoring_warehouse/detect_pipeline_dependency_gap.py` baru (Keputusan B). Step baru `monitoring-warehouse-dq-anomaly.yml`.
Result: worked, dengan 1 temuan penting saat verifikasi live data (Task 10) -- dicatat sebagai limitation, bukan bug yang diperbaiki sekarang (lihat di bawah).

## 2026-08-11 -- Temuan verifikasi (bukan error, limitation by design)
Encountered: Query `alerts_with_root_cause` terhadap data live hari ini (2026-08-11) menunjukkan 2 alert NYATA milik M6.6 (`serving_swap_orphan_table` di Titik 10, dari deteksi 73-orphan asli; `serving_swap_slow` di Titik 8, dari uji coba lock terkontrol M6.6) -- keduanya `is_simulated=false`, keduanya benar-benar terjadi hari ini (sisa kerja verifikasi M6.6), TAPI sama sekali tidak berkaitan sebab-akibat satu sama lain. Karena Titik 10 depends_on Titik 8, dan keduanya kebetulan sama-sama punya event "hari ini", view mengelompokkan alert orphan-table Titik 10 sebagai "disebabkan oleh" Titik 8 (root_titik_id=8) -- atribusi yang salah secara kausal, meski tidak crash/error.
Cause: Korelasi berbasis "hari kalender yang sama" (`snapshot_date`/`triggered_at::date = CURRENT_DATE`, pola konsisten seluruh milestone 6.x) cukup untuk skenario KK2 yang sebenarnya (kaskade nyata terjadi dalam 1 run pipeline terjadwal yang sama, jadi otomatis 1 hari yang sama) -- tapi False Positive muncul kalau 2 alert NYATA-TAPI-TIDAK-TERKAIT kebetulan sama-sama aktif di hari yang sama dan kebetulan berbagi edge dependency (persis kasus di sini: 2 temuan asli M6.6 yang berbeda akar, sama-sama masih "hari ini" karena verifikasi M6.6 baru selesai kemarin/hari ini).
Fix: TIDAK diperbaiki -- dicatat sebagai limitation di `decisions.md`/`report.md` Known Gaps, bukan bug. Melacak rantai kausal sungguhan (mis. lewat `run_id`/waktu presisi antar titik) adalah scope jauh lebih besar dari yang diminta KK2 ("satu alert jelas menunjukkan akar" -- terpenuhi untuk skenario 1-akar 1-hari yang jadi fokus KK2, termasuk uji coba terkontrol Checkpoint 5), dan kasus yang ditemukan di sini murni artefak 2 event nyata M6.6 yang kebetulan masih ada di tabel yang sama, bukan gejala pipeline nyata yang salah.
Verifikasi lanjutan: `detect_pipeline_dependency_gap.py` dry-run (`persist=False`) terhadap data live -- hasil kosong (`[]`), dikonfirmasi lewat query manual `pipeline_run_log` bahwa Titik 1 maupun Titik 2 memang belum jalan hari ini (workflow terjadwal 03:00/05:00 UTC belum lewat saat verifikasi) -- perilaku benar (tidak ada yang perlu dicek kalau Titik 2 belum jalan).

## 2026-08-11 -- Checkpoint 3
Did: `scripts/grafana/build_dashboard_warehouse_serving.py` (baru) -- dashboard "Nirwana - Warehouse & Serving Monitoring (Fase 2)", 9 panel, datasource Postgres existing (M1.5). Provisioned nyata ke Grafana Cloud (status 200, sukses).
Result: worked, dengan 3 bug ditemukan+diperbaiki saat verifikasi (lihat di bawah) SEBELUM commit -- bukan dilaporkan selesai lalu ditemukan salah belakangan.

## 2026-08-11 -- Error (ditemukan saat verifikasi, diperbaiki sebelum commit)
Encountered: Verifikasi manual tiap query panel terhadap data live (Task 13) menemukan 3 panel (Model Staleness di P5, Reverse ETL di P6, Storage di P7) ikut menampilkan baris data SIMULASI/UJI COBA dari milestone SEBELUMNYA (M6.4, M6.6) yang tersisa di tabel: `ml_model_version_snapshot` masih ada baris `model_name='sim_model'` (marker M6.4, snapshot_date=hari ini); `reverse_etl_sync_log` masih ada 9 baris `dataset_name='_simulation'` DENGAN `is_simulated=FALSE` (harusnya TRUE -- bug nyata di `serving_layer_monitor/simulate_test.py` M6.6, ditemukan di sini, bukan diperbaiki di sana); `serving_storage_snapshot` juga ada 1 baris `schema_name='_simulation'`.
Cause: Panel baru saya HANYA memfilter `is_simulated=false` (mengasumsikan kolom itu selalu benar) tanpa filter tambahan `schema_name != '_simulation'` -- padahal `CLAUDE.md` sudah eksplisit memperingatkan pola ini ("Any query or view against monitoring.* must filter out schema_name = '_simulation' -- ... ini sudah terjadi sekali") DAN membuktikan kasus M6.6 di atas persis kejadian itu terulang lagi (is_simulated=FALSE yang seharusnya TRUE, root cause ada di script test M6.6, bukan di sini).
Fix: Tambah `AND dataset_name/schema_name != '_simulation'` eksplisit di P6/P7, dan `WHERE model_name != 'sim_model'` (marker M6.4 sendiri, dikonfirmasi dari `SIM_MODEL_NAME` di `monitoring_warehouse/simulate_test.py`) di P5 -- pola dual-filter (bukan cuma andalkan 1 kolom) sesuai instruksi eksplisit CLAUDE.md. TIDAK memperbaiki root cause di `serving_layer_monitor/simulate_test.py` (M6.6, sudah closed milestone) -- di luar scope M6.7, dicatat sebagai temuan di report.md nanti.
Verifikasi: query manual seluruh 8 panel SQL (P3-P8) ulang setelah fix -- 0 baris `_simulation`/`sim_model` lagi di P5/P6/P7 (P7 sekarang tepat 2 baris: `mart_cleaned` 23 tabel 0 orphan, `mart_aggregated` 77 tabel 0 orphan -- cocok laporan M6.6 pasca-cleanup). Dashboard di-reprovision (version 2), status 200 sukses. Panel 1/2 (titik status + root cause) sudah diverifikasi bersih di Checkpoint 2. P1 dicek eksplisit tidak kena leakage `pipeline_run_log`'s trigger_event='simulated' rows (M6.3 marker) karena semuanya bertanggal masa lalu (1-6 Agustus), kalah oleh run nyata yang lebih baru di `DISTINCT ON ... ORDER BY completed_at DESC`.

## 2026-08-11 -- Checkpoint 4
Did: `scripts/grafana/create_alerts_warehouse_serving.py` (baru) -- SATU rule "Alert Aktif per Titik Akar Masalah (Fase 2, M6.7)" bersumber `monitoring.alerts_with_root_cause`, `ruleGroup` baru `warehouse-serving-monitoring` di folder "Nirwana Monitoring" existing (reuse `_get_or_create_folder` dari `create_alerts.py`, bukan duplikat). Query mengembalikan 1 baris per `root_titik_id` (pola multi-dimensional alert Grafana -- tiap baris jadi 1 alert instance, kolom non-value otomatis jadi label) -- SENGAJA bukan 1 rule per 13 alert_type (itu sendiri akan membanjiri). Label `source=nirwana-warehouse-serving` dipasang di rule, dipakai notification policy baru (nested route, `group_by: [root_titik_id]`) supaya alert dengan root sama dibundel jadi 1 notifikasi.
Result: worked. Rule ter-provision (201), policy ter-update (202).
Verifikasi: `GET /api/v1/provisioning/alert-rules` -- 3 rule total (2 Fase 1 utuh, `uid`/`ruleGroup`/`folder` sama persis; 1 rule baru M6.7 di group baru). `GET /api/v1/provisioning/policies` -- root policy (`receiver=empty`, `group_by=[grafana_folder, alertname]`) TIDAK berubah, 1 route baru ditambah dengan matcher+group_by yang benar, `receiver` route baru tetap `empty` (Keputusan E -- kanal eksternal tetap ditunda, tidak ada channel nyata yang terhubung).

## 2026-08-11 -- Checkpoint 5, Task 17-19 -- Error signifikan, 2 putaran perbaikan
Did: Injeksi sintetis Titik 2 (root, pipeline_run_log gagal) + Titik 3/6/7/9 (downstream, monitoring.alerts is_simulated=TRUE) hari ini, verifikasi `alerts_with_root_cause`.

## 2026-08-11 -- Error #1
Encountered: `downstream=[]` -- kosong total, root_row ditemukan benar tapi 4 event downstream sama sekali tidak muncul.
Cause: `titik_event_today` (Checkpoint 2) hardcode `WHERE is_simulated = FALSE` -- filter yang BENAR untuk konsumsi dashboard aman, tapi membuat mekanisme korelasinya sendiri MUSTAHIL diuji coba terkontrol (karena data uji coba SELALU `is_simulated=TRUE`). Persis tegangan desain yang sudah didokumentasikan `detect_parity_mismatch.py` (view aman-default vs detector butuh akses eksplisit) -- tapi di sana solusinya "bypass view, query tabel langsung" tidak berlaku di sini karena view ini gabungan 3 tabel (bukan 1), logic korelasinya SENDIRI yang mau diuji, bukan bisa direplikasi terpisah di Python.
Fix: Refactor `titik_event_today`/`alerts_with_root_cause` -- BUKAN memfilter is_simulated di view, tapi MENGEKSPOSNYA sebagai kolom (diturunkan: `alerts.is_simulated` langsung; `pipeline_run_log` dari `trigger_event='simulated'`; `dbt_test_result` di-hardcode FALSE karena tabel itu tidak punya kolom ini DAN tidak ada konvensi injeksi sintetis untuknya, M6.3 decisions.md). Filter "aman untuk dashboard" (`is_simulated=false`) dipindah ke titik KONSUMSI -- panel 2 `build_dashboard_warehouse_serving.py` dan rule `create_alerts_warehouse_serving.py`, keduanya di-reprovision (dashboard version 3). `CREATE OR REPLACE VIEW` ditolak Postgres saat kolom baru disisipkan di tengah (bukan di akhir) daftar kolom (`InvalidTableDefinition`) -- diganti `DROP VIEW IF EXISTS` + `CREATE VIEW` biasa. Skenario uji coba juga diubah: Titik 3/7 pindah dari `monitoring.dbt_test_result` (tidak ada kolom/konvensi is_simulated) ke `monitoring.alerts` dengan `alert_type='dbt_test_failure'` (sekaligus menguji cabang CASE mapping yang sebelumnya tidak pernah terlewati data apa pun).

## 2026-08-11 -- Error #2
Encountered: Setelah fix #1, root_row benar tapi downstream salah: Titik 6/7 malah dapat `root_titik_id=5` (bukan 2), dan ada baris asing titik_id=5 (2x) serta titik_id=10 ikut muncul.
Cause: Skenario 1-6 (M6.3/M6.4) berjalan LEBIH DULU dalam `run_simulation()` yang SAMA, masing-masing menyisakan baris `is_simulated=TRUE` di `monitoring.alerts`/`pipeline_run_log` bertanggal HARI INI juga (mis. skenario 4 "sensor duration" push alert `ml_output_freshness_delay` dengan `snapshot_date=today` -- align ke Titik 5). Karena view Checkpoint 2 sekarang TIDAK memfilter is_simulated sama sekali (fix #1), SEMUA skenario lain yang kebetulan aktif "hari ini" ikut masuk graph korelasi skenario 7 -- Titik 6 depends_on {2, 5}, jadi ketika Titik 5 JUGA punya event (dari skenario 4), pencarian leluhur-tertinggi jadi ambigu (dua jalur sama-sama depth 1, `DISTINCT ON ... ORDER BY depth DESC` menang secara tidak stabil/acak di antara keduanya). Ini KELAS masalah yang SAMA dengan temuan Checkpoint 2 ("2 alert nyata M6.6 tak terkait ikut terkorelasi") -- kali ini muncul ANTAR-SKENARIO simulate_test.py sendiri, bukan antar-milestone.
Fix: Tambah `_cleanup(cur); conn.commit()` di AWAL blok Skenario 7 (sebelum insert data sendiri) -- skenario 1-5 sudah selesai di-assert di titik itu, aman dibersihkan supaya Skenario 7 berjalan di graph yang bersih/terisolasi. TIDAK mengubah logic view lebih lanjut (perbaikan algoritma buat kasus multi-skenario-simultan di luar scope KK2 -- sudah didokumentasikan sebagai limitation yang sama sejak Checkpoint 2).
## 2026-08-11 -- Checkpoint 6
Did: `api/app/queries.py` extend (8 query agregat Fase 2) + `api/app/main.py` extend (8 route baru `/api/warehouse/*`). Verifikasi 2 lapis: (1) query langsung lewat `monitoring_api_reader` (Keputusan H) -- 8/8 sukses TANPA grant tambahan, dikonfirmasi empiris; (2) HTTP nyata via `uvicorn` lokal -- 8/8 endpoint HTTP 200 dengan data benar.
Result: worked, dengan 1 temuan BARU signifikan saat verifikasi HTTP (Task 24) -- root cause BEDA dari 2 temuan Checkpoint 3 sebelumnya, dicatat terpisah.

## 2026-08-11 -- Temuan baru (race condition, BUKAN bug wiring is_simulated)
Encountered: `/api/warehouse/volume-anomaly` (data live nyata lewat HTTP) menampilkan 2 baris `dataset_name='_simulation', table_name='sim_table_a'/'sim_table_b'` DENGAN `is_simulated=FALSE` -- padahal `simulate_test.py` Skenario 1 eksplisit memanggil `run_volume_detection(..., is_simulated=True)`.
Cause: DIBACA KODENYA (`detect_volume_anomaly.py`) -- wiring `is_simulated` benar, TIDAK ada bug logic di situ. Root cause sebenarnya RACE CONDITION: `simulate_test.py` Skenario 1 sempat menulis baris sintetis ke `monitoring.warehouse_volume_snapshot` (dataset `_simulation`) sebelum cleanup di run berikutnya -- kalau workflow terjadwal NYATA (`monitoring-warehouse-dq-anomaly.yml`, `workflow_run` listener) kebetulan jalan DI ANTARA itu, `detect_volume_anomaly.py --main` (`list_tables_for_date`, tidak mengecualikan `_simulation`) ikut memindai baris sintetis itu sebagai tabel biasa dan (benar dari sudut pandang kodenya sendiri, yang jalan sebagai proses PRODUKSI asli) menulis alert `is_simulated=FALSE` -- karena proses itu memang tidak tahu-menahu bahwa datanya sintetis. Baris hasil race condition ini KEBAL dari `_cleanup()` manapun (semua filter `is_simulated=TRUE`, baris ini FALSE) -- akan menumpuk permanen kalau tidak ditangani.
Fix: TIDAK memperbaiki `detect_volume_anomaly.py` (di luar scope M6.7, kode M6.3 yang sudah closed) -- filter defensif `AND schema_name != '_simulation'` ditambah ke `WAREHOUSE_VOLUME_ANOMALY` (api/) DAN panel 4 dashboard (`build_dashboard_warehouse_serving.py`, di-reprovision version 4), pola sama 3 temuan `_simulation` sebelumnya. 2 baris yang sudah terlanjur ada di `monitoring.alerts` dihapus manual (cleanup satu kali, tidak bisa lewat script existing manapun karena kebal `is_simulated=TRUE` filter). Ditemukan JUGA (tidak baru, sudah didokumentasikan Checkpoint 5): `serving_swap_orphan_table` detail text masih menyebut `_simulation.fake_table__old` (dari M6.6, bukan filter-able lewat kolom schema_name karena schema_name alert itu sendiri `'serving_layer'` bukan `_simulation`) -- dibiarkan, murni informational, akar masalah di `detect_orphan_tables.py` (M6.6).
Ditambahkan `docs/keputusan-tertunda.md` entri baru untuk race condition ini (beda dari 2 temuan `_simulation` leak Checkpoint 3, yang itu murni "kolom tidak reliable" -- ini "proses production nyata bisa scan data test kalau timing kebetulan tumpang tindih").
Verifikasi ulang: HTTP `/api/warehouse/volume-anomaly` -> `[]` (bersih). `/api/warehouse/alerts` masih ada 2 baris menyebut `_simulation.fake_table__old` di teks detail (diterima, informational, root cause M6.6).

## 2026-08-11 -- Checkpoint 7
Did: Cek `node_modules/next/dist/docs/.../fetch.md` (Task 26, per `web/AGENTS.md`) -- `options.next.revalidate` masih sama seperti dipakai `web/src/lib/api.ts` (M1.7), tidak ada breaking change. `web/src/lib/api.ts` extend (8 tipe + 8 fungsi client). 4 halaman baru (`/warehouse`, `/warehouse/dq-volume-ml`, `/warehouse/reverse-etl-serving`, `/warehouse/chatbot-performance`), `Nav.tsx` extend (4 link baru). `.claude/launch.json` dibuat (belum ada sebelumnya) untuk `preview_start` jalankan `next dev`.
Result: worked, dengan 2 temuan lagi saat verifikasi browser nyata (Task 31) -- keduanya diperbaiki sebelum commit.

## 2026-08-11 -- Temuan: leftover test residue dari pendekatan Checkpoint 5 yang ditinggalkan
Encountered: Halaman `/warehouse` (data live via browser) menampilkan "DQ gate mart_cleaned gagal: sim_test_titik3"/"...titik7" di panel alert -- padahal Skenario 7 versi FINAL (setelah refactor is_simulated, lihat entri Checkpoint 5 Error #1) tidak lagi memakai `monitoring.dbt_test_result` sama sekali.
Cause: Percobaan PERTAMA (sebelum refactor) sempat insert 2 baris ke `monitoring.dbt_test_result` (`test_name='sim_test_titik3'/'sim_test_titik7'`) beserta cleanup-nya -- saat direfactor ke pendekatan `monitoring.alerts`, baris cleanup untuk `dbt_test_result` ikut dihapus dari kode TAPI 2 baris yang sudah terlanjur ter-insert sebelumnya tidak pernah dihapus dari database -- karena `dbt_test_result` tidak punya kolom is_simulated (desain view: selalu FALSE), baris ini permanen "terlihat nyata".
Fix: Hapus manual 2 baris tsb (`DELETE FROM monitoring.dbt_test_result WHERE test_name IN (...)`).

## 2026-08-11 -- Temuan: monitoring.pipeline_run_status (M6.2) tidak simulation-aware
Encountered: Panel "Status 10 Titik" menampilkan Titik 2 = **failure** (bukan success) -- padahal itu baris SIMULASI Skenario 7 (`trigger_event='simulated'`) yang belum sempat dibersihkan (uji coba Checkpoint 5 baru selesai, cleanup dijadwalkan "run berikutnya" sesuai konvensi, bukan langsung).
Cause: `WAREHOUSE_PIPELINE_STATUS` (Checkpoint 6) memakai `monitoring.pipeline_run_status` (view M6.2) apa adanya -- view itu `DISTINCT ON (titik_id) ORDER BY completed_at DESC` TANPA kolom/filter trigger_event sama sekali (M6.2 tidak pernah didesain untuk diekspos terus-menerus ke dashboard publik, cukup query manual sesekali). Baris simulasi Skenario 7 (completed_at lebih baru dari run nyata terakhir) "menang" jadi status ditampilkan.
Fix: `WAREHOUSE_PIPELINE_STATUS` (api/) dan panel 1 dashboard (`build_dashboard_warehouse_serving.py`) DIUBAH untuk query `monitoring.pipeline_run_log` LANGSUNG (bukan lewat view `pipeline_run_status`), mereplikasi `DISTINCT ON` yang sama + tambah `WHERE trigger_event != 'simulated'`. Tidak menyentuh view/schema M6.2 itu sendiri (di luar scope, closed milestone) -- pola sama "consumer bypass + filter defensif" yang sudah dipakai 3x sebelumnya di checkpoint ini.
Juga dijalankan: `_cleanup()` dari `simulate_test.py` (membersihkan baris simulasi Skenario 7 yang sudah selesai diverifikasi, tidak perlu ditahan lagi).
Verifikasi ulang (browser nyata, `next dev` lokal -> `uvicorn` lokal): SEMUA 4 halaman baru + 6 halaman Fase 1 existing dicek -- data bersih, Titik 2 = success, 0 titik gagal hari ini, root-cause grouping menampilkan data NYATA (bukan sintetis lagi): Titik 8 (`serving_swap_slow`, M6.6) sebagai akar, Titik 10 (`serving_swap_orphan_table`) sebagai downstream -- bukti tambahan KK2 bekerja di data produksi asli, bukan cuma skenario buatan. Ditemukan (bukan bug, gap M5.7/M6.6 yang SUDAH terdokumentasi): tabel `mart_cleaned`/`mart_aggregated` mulai "kept (orphan)" lagi pasca reverse ETL run terjadwal terbaru -- persis prediksi `report.md` M6.6 ("orphan baru akan menumpuk lagi tanpa reapply view manual"), justru bukti `detect_orphan_tables.py` bekerja benar. Nav bar 10 link (6 lama + 4 baru) dikonfirmasi lewat `read_page`. Halaman Fase 1 (`/`) dicek tetap utuh -- tidak terpengaruh perubahan M6.7.
**Catatan teknis untuk sesi berikutnya**: `next dev` (Turbopack) menyimpan fetch cache di disk (`web/.next/cache`), BUKAN cuma memori proses -- restart dev server SAJA tidak cukup untuk lihat data baru dalam window `revalidate: 300`, harus hapus folder `.next` juga.

## 2026-08-11 -- Checkpoint 8
Did: Verifikasi commit terpisah bersih per repo (Keputusan J) sebelum push -- dikonfirmasi eksplisit lewat permintaan user ("pastikan commit untuk bagian api/ dan web/ sesuai dengan reponya"): `git log --stat` 3 repo dicek, tidak ada file api/web yang nyasar ke commit nirwana-database, dan sebaliknya. 3 `AskUserQuestion` terpisah (Keputusan J) -- user setuju push ketiganya (`nirwana-database`, `nirwana-monitoring-api`, `nirwana-monitoring-web`), dan memilih "Saya deploy manual sendiri" untuk status deploy Vercel `web/` (bukan CLI, bukan tunda).
Push 3 remote sukses. Verifikasi live NYATA: `https://nirwana-monitoring-api.onrender.com/api/warehouse/*` -- 200, data benar, endpoint Fase 1 lama tetap utuh. `web/` (`nirwana-monitoring-web`) TERPUSH ke GitHub, TAPI belum di-deploy publik (Vercel) -- menunggu aksi manual user, dikonfirmasi akan dilakukan terpisah. `docs/keputusan-tertunda.md` diupdate ulang (Keputusan E, kanal notifikasi tetap Open, sekarang eksplisit mencakup kedua folder Grafana).
Result: Bagian internal (Grafana, Checkpoint 1-5) dan `api/` publik -- selesai+live+terverifikasi. `web/` publik -- kode selesai+terverifikasi lokal+terpush, deploy publik MENUNGGU user. `report.md` M6.7 ditulis mencerminkan status ini apa adanya (bukan diklaim selesai penuh sebelum benar-benar live) -- `milestones/1.7-public-monitoring-web/report.md` DITUNDA sampai deploy dikonfirmasi (Task 36, kondisional).

Verifikasi akhir: `simulate_test.py` penuh (8 skenario) -- SEMUA PASS, termasuk Skenario 7: `root_row=(2, 2, True)`, `downstream=[(3, 2), (6, 2), (7, 2), (9, 2)]` -- persis ekspektasi (1-hop Titik 3/6/9 dan 2-hop Titik 7 semua ke root Titik 2). Verifikasi TAMBAHAN (Task 19): query bentuk PERSIS query rule Grafana (`SELECT root_titik_id, COUNT(*) ... GROUP BY root_titik_id`) terhadap data sintetis sebelum cleanup -- hasil TEPAT 1 baris (`root_titik_id='2', value=5.0`) -- membuktikan Grafana akan menghasilkan SATU alert instance untuk seluruh 5 event (bukan 5 terpisah), inti pembuktian KK2. Panel dashboard/alert rule TIDAK menampilkan data sintetis ini secara visual (BY DESIGN -- keduanya filter `is_simulated=false`, pola identik dashboard Fase 1 M1.5 yang juga tidak pernah menampilkan hasil uji coba terkontrolnya sendiri) -- verifikasi query langsung di atas dinilai cukup, konsisten presedan seluruh milestone 6.x sebelumnya (deteksi dibuktikan lewat assert Python, bukan screenshot dashboard).
