# Milestone 6.6 — Execution Log

## 2026-08-11 — Plan mode: riset + 2 diskusi keputusan material (2 putaran)
Did: Riset langsung ke kode (`sync.py` kedua versi, `reindex_analyze.py`, `test_no_downtime_swap.py`) + verifikasi read-only langsung ke serving project (`pg_stat_user_tables`, `pg_depend`, `pg_total_relation_size`, `gh run list`). Ditemukan: 72/76 tabel `mart_aggregated` orphan `__old` (112MB), diikat SELURUH `analyst_views`+`chatbot_views`, `old_table_status` sudah dihitung tapi dibuang, mart_cleaned tidak punya fix graceful-degradation M5.7.
2 pertanyaan diajukan putaran 1 — user minta riset lebih dalam dulu ("cek lagi lebih spesifik", "kita diskusikan lebih lanjut"), TIDAK langsung jawab. Riset tambahan: cakupan dependency (`pg_depend`, 42+44 view), verifikasi dampak data-correctness aktual (live vs `__old` row count identik di 4 tabel, konsisten dataset statis project ini). Disampaikan lengkap ke user, putaran 2: user pilih instrumentasi penuh + cleanup sekali.
Result: worked. Plan ditulis lengkap dengan Keputusan A-B (dikunci user) + 6 keputusan derived.

## 2026-08-11 — Checkpoint 1 (Task 1-3): decisions.md + kredensial + schema
Did: Tulis `decisions.md`. `scripts/serving_layer_monitor/{connections.py, verify_role_isolation.py, db.py, setup_storage_reader.py}` — kredensial baru `serving_storage_reader` (serving), `pg_monitor` + `USAGE` `mart_cleaned`+`mart_aggregated`, TANPA SELECT tabel (kredensial paling sempit di project ini — cuma metadata/stats). `schema.sql` — `monitoring.serving_storage_snapshot` (is_orphan dihitung saat insert, sumber kebenaran tunggal utk deteksi orphan), extend `reverse_etl_sync_log` (+`old_table_status`, +`swap_duration_ms`), extend `alerts.alert_type` (+`serving_swap_orphan_table`, +`serving_swap_slow`).
Result: worked. 8/8 isolation checks OK, `USAGE`-only (tanpa `SELECT`) dikonfirmasi cukup untuk `pg_total_relation_size()` (sesuai prediksi riset). Schema diverifikasi `information_schema`.

## 2026-08-11 — Checkpoint 2 (Task 4-6): instrumentasi sync.py (fix)
Did: `scripts/reverse_etl_mart_aggregated/sync.py` — timer `time.monotonic()` di sekitar blok RENAME+DROP saja (bukan seluruh `sync_table()`), capture `old_table_status`+`swap_duration_ms` ke `log_sync_result()`. `scripts/reverse_etl/sync.py` (mart_cleaned) — port PERSIS try/except graceful-degradation dari versi mart_aggregated (M5.7) yang sebelumnya tidak ada, plus instrumentasi sama.

Task 6 verifikasi: `sync.py --table dim_shift_type` (mart_aggregated) dan `sync.py --table properties` (mart_cleaned) — keduanya tabel BEBAS orphan (dicek dulu via query, supaya tidak kena collision RENAME dengan `__old` yang sudah ada duluan — 72/76 tabel `mart_aggregated` dan `employees` di `mart_cleaned` PUNYA orphan existing, akan crash di step RENAME kalau dipakai test, BUKAN soal fix yang baru dibuat, beda kelas masalah). Hasil: `old_table_status='dropped'`, `swap_duration_ms` terisi (818.54ms, 959.87ms).

**Verifikasi TAMBAHAN, lebih lengkap dari rencana**: untuk benar-benar membuktikan fix mart_cleaned yang BARU di-port (bukan cuma port kode tanpa bukti jalan), dibuat skenario dependency-conflict NYATA: (1) `python scripts/chatbot_views/apply_views.py --all` (melepas dependency 3 view `chatbot_views` -- `v_lookup_employee_performance`, `v_lookup_payroll`, `v_lookup_staff_shifts` -- dari `employees__old` LAMA yang sudah ada sejak sebelum milestone ini, dikonfirmasi via `pg_depend`); (2) admin `DROP TABLE mart_cleaned.employees__old` (sekarang bisa, dependency sudah lepas); (3) `sync.py --table employees` -- karena view baru saja di-reapply ke versi live SEBELUM swap ini, swap ini otomatis menciptakan orphan BARU yang langsung tidak match reapply (Postgres view bind ke OID, bukan nama) -- DROP `employees__old` (baru) gagal `DependentObjectsStillExist` PERSIS seperti histori nyata M6.2, TAPI kali ini graceful: `WARNING: employees__old kept...` dicetak, sync tetap lanjut sukses (`755=755`), TIDAK crash seperti versi lama.
Result: **worked, fix mart_cleaned terbukti bekerja end-to-end dengan skenario nyata** (bukan sintetis) -- `reverse_etl_sync_log` untuk `employees` menunjukkan `status='synced'`, `old_table_status='kept (analyst_views/chatbot_views dependency...)'`, `swap_duration_ms=890.84`.

## 2026-08-11 — Checkpoint 3 (Task 7-8): storage growth + vacuum snapshot (KK1)
Did: `snapshot_serving_storage.py` -- query `pg_stat_user_tables` (size via `pg_total_relation_size`, live/dead tuple, last_vacuum/autovacuum) untuk SEMUA tabel `mart_cleaned`+`mart_aggregated` via `serving_storage_reader`, upsert `serving_storage_snapshot` (`is_orphan` dihitung dari suffix `__old`).
Result: worked. 173 tabel tersimpan (531.6MB total), **73 orphan tepat terdeteksi (112.1MB)** -- cocok persis temuan riset awal, membuktikan snapshot ini akan jadi sumber kebenaran yang benar untuk `detect_orphan_tables.py` (Checkpoint 4). Spot-check: `bookings` (38MB, `last_autovacuum` terisi -- autovacuum jalan normal), `dim_property` (tabel kecil, belum pernah divacuum -- wajar untuk tabel sekecil itu).

## 2026-08-11 — Checkpoint 4 (Task 9-12): swap health detection (KK2)
Did: `detect_orphan_tables.py` -- baca `serving_storage_snapshot.is_orphan` (Keputusan C), 1 alert agregat (bukan per-tabel) kalau ada orphan. Dijalankan SEBELUM cleanup (sengaja, sesuai plan).
Result: worked. **73 orphan NYATA terdeteksi tepat** (bukan sintetis) -- alert CRITICAL, detail berisi daftar tabel + total size 112.1MB.

`detect_swap_duration_anomaly.py` -- replikasi PERSIS algoritma rolling-baseline M6.3, per (dataset_name, table_name) supaya tidak salah kaprah bandingkan tabel besar vs kecil.

Task 11 uji coba terkontrol slow-swap: bangun baseline dulu (5 sync normal `dim_shift_type`, ~800-1100ms). Coba lock pakai koneksi ADMIN (`serving_layer_monitor/connections.py::get_serving_connection`) -- **gagal** `permission denied for table dim_shift_type` (temuan M3.5 soal admin tidak punya privilege ke tabel yang tidak dimilikinya, terbukti lagi, kali ini utk LOCK bukan GRANT). Diperbaiki: pakai kredensial OWNER (`reverse_etl_mart_aggregated_writer`, via `scripts/reverse_etl_mart_aggregated/connections.py::get_serving_writer_connection`) -- SAMA kredensial yang dipakai `sync.py` sendiri. Percobaan pertama (lock 6 detik) TIDAK menangkap delay (subprocess startup Python + BigQuery client auth ternyata makan waktu lebih dari 6 detik, lock sudah lepas duluan sebelum RENAME sempat coba) -- diperbaiki naikkan ke 20 detik.
Result: worked. `swap_duration_ms=10518.7ms` tertangkap (vs baseline mean=914.1ms) -- lock beneran menahan RENAME, bukan angka sintetis. Detector: `[CRITICAL] z=71.43`.

Task 12: `test_no_downtime_swap.py` mart_cleaned -- 300 query konkuren, 0 error, PASSED. mart_aggregated -- **DITUNDA**: script hardcode tabel `dim_property`, yang masih punya orphan `dim_property__old` (belum dibersihkan Task 13) -- `sync_table()` crash `DuplicateTable` di baris RENAME PERTAMA, sebelum sempat menguji apa pun. Bukti independen lagi bahwa 72 orphan `mart_aggregated` bukan cuma soal storage, tapi AKTIF menghalangi operasi normal (termasuk test regresi milestone lama). Akan di-re-run setelah Task 13 (cleanup) selesai.

## 2026-08-11 — Checkpoint 5 (Task 13-14 + lanjutan Task 12): cleanup + verifikasi ulang
Did: Reapply `scripts/data_analyst_views/apply_views.py --all` + `scripts/chatbot_views/apply_views.py --all` (melepas dependency SEMUA 73 orphan sekaligus). Tulis `scripts/serving_layer_monitor/cleanup_orphan_tables.py` (baru, `--dry-run` + eksekusi nyata) -- dry-run konfirmasi 73 tabel persis sama dengan temuan riset awal, eksekusi nyata: 73/73 berhasil di-DROP.
Result: worked. Verifikasi before/after: orphan count 73->0; live table row count UTUH (`dim_property`=6, `fact_revenue_property_daily`=5485, `dim_employee`=755, `mart_cleaned.employees`=755 -- semua sama persis sebelum cleanup); view `chatbot_views`/`analyst_views` masih berfungsi lewat tabel live yang benar (dikonfirmasi query langsung 3 view lintas 2 schema).

Task 14 verifikasi ulang: `snapshot_serving_storage.py` + `detect_orphan_tables.py` pasca-cleanup. **1 bug nyata ditemukan+diperbaiki**: run pertama pasca-cleanup MASIH menunjukkan 73 orphan (SALAH) -- diagnosis: snapshot pra-cleanup dan pasca-cleanup terjadi di HARI KALENDER YANG SAMA, `ON CONFLICT (schema_name, table_name, snapshot_date) DO UPDATE` cuma meng-update baris utk tabel yang MASIH ADA, baris utk 73 tabel yang sudah di-DROP tetap tertinggal (stale) di snapshot hari itu karena tidak pernah "dihapus". `detect_orphan_tables.py` defaultnya pakai `datetime.now().date()` ("hari ini"), jadi menemukan baris stale itu. Diperbaiki: ganti ke `MAX(snapshot_date)` dari tabel (bukan asumsi "hari ini") -- kelas bug sama persis timezone UTC-vs-lokal M6.3. Setelah hapus baris stale hari itu + snapshot ulang bersih + fix: `[ok] tidak ada tabel orphan terdeteksi`.

Lanjutan Task 12 (mart_aggregated `test_no_downtime_swap.py`, tertunda dari Checkpoint 4): dicoba lagi setelah cleanup -- **GAGAL LAGI, TAPI DENGAN GAP BERBEDA**: cycle 1/8 sukses (dengan WARNING wajar -- analyst_views belum di-reapply lagi setelah swap ini), tapi cycle 2/8 CRASH `DuplicateTable` di `ALTER TABLE ... RENAME TO "dim_property__old"` -- karena nama itu SUDAH DIPAKAI orphan dari cycle 1 (test_no_downtime_swap.py sengaja menjalankan 8 siklus BACK-TO-BACK tanpa reapply view di antaranya, sesuai desain aslinya M5.5). Ini gap BARU dan BERBEDA dari yang diperbaiki Checkpoint 2 (yang menangani kegagalan DROP karena dependency -- ini kegagalan RENAME karena tabrakan nama, satu level lebih awal). Di luar Keputusan A yang disetujui (cuma cakup DROP step) -- TIDAK diperbaiki di M6.6, akan di-flag sebagai follow-up terpisah (spawn_task). Orphan baru yang tercipta dari percobaan ini (1 `dim_property__old`) dibersihkan lagi (reapply `data_analyst_views`+`chatbot_views` -- awalnya cuma reapply `data_analyst_views`, ternyata `chatbot_views` JUGA depend, `cleanup_orphan_tables.py` gagal sekali sebelum ketahuan -- lalu reapply keduanya, sukses).

KK2 "zero downtime" tetap dianggap terverifikasi valid meski mart_aggregated tidak sempat re-run penuh 8 siklus: (a) mart_cleaned re-run PENUH sukses (300 query, 0 error) membuktikan instrumentasi timing M6.6 tidak mengubah mekanisme RENAME/DROP yang mendasarinya (kode swap byte-identik dengan sebelumnya, cuma dibungkus timer -- mekanisme locking Postgres yang sama persis dipakai kedua file); (b) bukti asli M5.5 (250 query, mart_aggregated) untuk mekanisme swap itu sendiri tetap valid, tidak disentuh perubahan M6.6.
