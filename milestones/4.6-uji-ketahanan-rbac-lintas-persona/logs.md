# Milestone 4.6 — Execution Log

## 2026-08-10 -- Mulai, breakdown via planning-and-task-breakdown skill
Did: Baca kontrak sumber (`05-serving-ai-chatbot.md` Milestone 4.6). Query langsung `mart_cleaned.role_permissions` (77 baris) dan `mart_cleaned.employees`/`chatbot_views.v_employees_directory` untuk membangun matriks ekspektasi 20×10 dan menyelidiki data uji `own_property`. Ditemukan `employees.role_title` NULL by design dan `department`/`access_level` cuma 8×2 bucket kasar -- tidak cukup granular memetakan 20 `role_title` `role_permissions` ke pegawai sungguhan satu-satu. Diinvestigasi lebih jauh: `authorize()` dan `resolve_property_id()` di `scripts/chatbot_api/` memang tidak pernah saling membaca parameter satu sama lain -- `role_title` dan `employee_id` adalah klaim independen, konsisten pemisahan Lapis 1/Lapis 2 M4.5. Ini disederhanakan jadi keputusan desain uji: 1 employee_id tetap per property cukup untuk seluruh sel own_property, apa pun role_title yang diklaim.
User minta dokumen rancangan pengujian terpisah dibuat DULU (matriks 20×10 lengkap) sebelum plan disetujui, dan direview eksplisit sebelum lanjut. Matriks dibangun manual dari 77 baris data (diverifikasi ulang cell-by-cell terhadap data mentah), disematkan penuh ke dalam plan file untuk ditinjau user.
Result: Plan (termasuk matriks 200-sel lengkap) disetujui user. `docs/09-serving-ai-chatbot/rancangan-pengujian-rbac-chatbot.md` dan `decisions.md` ditulis lengkap. Commit `cedcda8`.

## 2026-08-10 -- Checkpoint 1: tooling
Did: `scripts/chatbot_rbac_test/{connections.py,ground_truth.py,run_access_matrix.py}`. Tambah `requests==2.33.1` ke `requirements.txt` (verifikasi HTTP M3.4-4.5 sejauh ini selalu manual curl, M4.6 pertama kali butuh HTTP client terprogram untuk 200 kombinasi).
`uvicorn main:app --port 8010` (scripts/chatbot_api). Validasi `load_role_permissions()` -- 77 baris cocok jumlah `role_permissions`, 3 sampel dicek manual (CEO/reservation=all_properties, HR Staff/financial=None/DENY, General Manager/spa_event=own_property) -- seluruhnya cocok matriks rancangan. `pick_test_employee_ids()` -- E0001 (P01), E0166 (P02).
Dry-run 3 kombinasi lewat `call()` langsung terhadap API live: CEO/reservation -> 200 (expect 200), HR Staff/financial -> 403 (expect 403), General Manager/spa_event -> 200 (expect 200) -- 3/3 cocok.
Result: worked. Mekanika tooling (ground truth loader + HTTP call + perbandingan) tervalidasi sebelum full run 200 kombinasi.
