# Milestone 6.4 — Execution Log

## 2026-08-11 — Plan mode: riset + 2 diskusi keputusan material
Did: Explore agent riset `mock_score.py`/M5.4/dokumen arsitektur §9.4.2 & Bagian 10 + baca langsung `mock_score.py`. 2 keputusan diajukan ke user via `AskUserQuestion`: (1) pendekatan staleness mengingat `model_version` konstan hardcoded — user pilih informational-only + catatan `keputusan-tertunda.md`; (2) penanganan KK3 drift mengingat tidak ada data drift sama sekali — user minta penjelasan lebih detail dulu (diberikan breakdown 3 opsi + trade-off), lalu memilih canary check.
Result: worked. Plan ditulis lengkap dengan 2 keputusan terkunci + 4 keputusan derived (completeness pakai `mart_aggregated` bukan `mart_cleaned`, kredensial reuse+IAM tambahan, uji coba KK2 via fault-injection nyata, extend file existing).

## 2026-08-11 — Checkpoint 1 Task 1 & 3: decisions.md + schema
Did: Tulis `decisions.md` (kontrak, temuan riset, 2 diskusi user, 5 technical decisions). Extend `scripts/monitoring_warehouse/schema.sql`: `ml_model_version_snapshot` + view `ml_model_staleness_status`, `ml_output_completeness_snapshot`, `ml_output_missing_entity`, `ml_drift_data_availability_check`, extend `alerts.alert_type` CHECK (+`ml_output_incomplete_scoring`). Jalankan `apply_schema.py`.
Result: worked. Verifikasi `information_schema`: 4 tabel + 1 view baru live, CHECK constraint benar berisi 10 nilai (9 lama + 1 baru).

## 2026-08-11 — Checkpoint 1 Task 2: kredensial — diblokir classifier, menunggu user
Did: Coba jalankan `gcloud projects add-iam-policy-binding ... --role=roles/bigquery.metadataViewer` untuk `warehouse-monitor-reader` (kebutuhan `list_datasets()` project-wide, Keputusan D).
Result: **diblokir classifier auto-mode** (pola sama M2.1-2.3 dulu). Perintah exact diberikan ke user untuk dijalankan manual. Task 2 masih pending user action — TIDAK memblokir Task 4-9 (staleness & completeness cuma butuh grant `ml_output`/`mart_aggregated` READER yang sudah ada sejak M6.3), cuma memblokir Checkpoint 4 (drift canary, Task 10-11) yang butuh `list_datasets()`.

## 2026-08-11 — Checkpoint 2 (Task 4-6): Model staleness (KK1)
Did: Tulis `scripts/monitoring_warehouse/snapshot_ml_model_version.py` (GROUP BY model_name/model_version atas `ml_output.predictions`, upsert `ml_model_version_snapshot`). Jalankan terhadap data live. Tambah entri baru `docs/keputusan-tertunda.md` (threshold retrain cadence, Status Open).
Result: worked. Data live: `model_name=occupancy_forecast`, `model_version=occupancy_forecast_mock_v1`, `first_scored_at=2026-08-08 12:55:59`, `row_count_total=1512` (naik dari 756 M6.3 karena tambahan run selama M6.3). View `ml_model_staleness_status`: `days_since_first_scored=2`, `is_most_recently_active=true` — KK1 terpenuhi (bisa lihat kapan model_version ini pertama aktif tanpa tanya siapa pun), sesuai keputusan informational-only (tidak ada alert/threshold).

## 2026-08-11 — Checkpoint 3 (Task 7-9): Completeness validation (KK2)
Did: Tulis `scripts/monitoring_warehouse/check_ml_output_completeness.py`.
Result: **1 bug nyata ditemukan & diperbaiki saat Task 8** (baseline run pertama): query awal asumsi `ml_output.predictions` punya kolom `property_id`/`room_type_id` terpisah — ternyata cuma ada `entity_id` komposit (`"property_id:room_type_id"`), sama seperti temuan riset di `decisions.md` tapi lupa diterapkan ke query `fetch_scored_entities`. Error `BadRequest: Unrecognized name: property_id`. Diperbaiki pakai `SPLIT(entity_id, ':')` persis pola `fact_ml_occupancy_forecast_property_room_type.sql`. Baseline live setelah perbaikan: `feature_snapshot_at=2026-07-01`, `expected=18 scored=18 missing=0` (Task 8, KK2 baseline benar).

Task 9 (uji coba terkontrol, Keputusan E): **temuan desain penting** sebelum eksekusi — karena `mock_score.py` selalu pakai `last_date = MAX(period_date)` (statis, selalu sama) dan self-union (tidak pernah menghapus baris lama), sekadar exclude 1 property TANPA mengganti `last_date` tidak akan pernah kelihatan "missing" (baris lama entity itu dari run-run sebelumnya di `feature_snapshot_at` yang sama tetap ada). Solusi: tambah 2 CLI flag sementara ke `mock_score.py` (`--test-last-date`, `--test-exclude-property-id`) supaya test bisa memakai `feature_snapshot_at` yang BELUM PERNAH dipakai run manapun (2026-06-30, tanggal kedua-terbaru) — pengecualian P03 jadi genuinely terlihat "missing" untuk snapshot itu, sekaligus otomatis tidak pernah tersentuh kembali oleh detector normal (yang selalu pakai `MAX(feature_snapshot_at)` = 2026-07-01, tidak terpengaruh). Efek samping test ini permanen di BigQuery (DML blocked, tidak bisa dihapus) tapi harmless & tersegel sendiri karena tidak pernah jadi "latest" lagi.

Dijalankan: `mock_score.py --test-last-date 2026-06-30 --test-exclude-property-id P03` (lokal, kredensial `ml-scoring-writer` sudah ada di `.env`) → 210 baris baru (15 entity x 14 hari, P03 tidak ikut). Ditambah `--feature-snapshot-at`/`--simulated` CLI flag PERMANEN (bukan sementara) ke `check_ml_output_completeness.py` supaya bisa audit snapshot spesifik, bukan cuma "yang terbaru" — dipakai untuk test ini: `check_ml_output_completeness.py --feature-snapshot-at 2026-06-30T00:00:00 --simulated` → `expected=18 scored=15 missing=3`, tepat `[('P03',1),('P03',2),('P03',3)]`. Alert `monitoring.alerts` (`is_simulated=true`) dan 3 baris `ml_output_missing_entity` terverifikasi benar via query langsung.

`mock_score.py` direvert penuh (`git checkout --`), dikonfirmasi `git status --short` bersih.

## 2026-08-11 — Checkpoint 4 Task 10: dicoba verifikasi kredensial Task 2, TERNYATA BELUM digrant
Did: Test cepat `list_datasets()` dengan `warehouse-monitor-reader` — hasilnya 4 dataset (`mart_aggregated`,`mart_cleaned`,`ml_output`,`raw_production`), awalnya terlihat seperti Task 2 sudah beres.
Result: **Ternyata BUKAN bukti Task 2 selesai** — `gcloud projects get-iam-policy ... --filter=bindings.members:warehouse-monitor-reader` (read-only, tidak diblokir classifier) mengonfirmasi cuma `roles/bigquery.jobUser` yang ada, BELUM ada `roles/bigquery.metadataViewer`. 4 dataset yang muncul ternyata berasal dari 4 ACL per-dataset M6.3 yang sudah ada sebelumnya (dataset READER ACL ternyata cukup untuk `datasets.get` per-dataset itu sendiri, tapi TIDAK untuk melihat dataset LAIN yang tidak di-ACL — dikonfirmasi ground truth `bq ls` project sungguhan ada 7 dataset total, 3 hilang dari hasil `list_datasets()`: `mart_aggregated_staging`, `mart_cleaned_staging`, `staging`). Ini justru memvalidasi premis Keputusan D (kenapa IAM project-level tambahan benar-benar dibutuhkan, bukan cuma teori). Task 2 masih menunggu user menjalankan perintah `gcloud` yang diberikan — Checkpoint 4 (Task 10-11) di-pause sampai itu selesai.
