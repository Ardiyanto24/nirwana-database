# Milestone 1.5 — Execution Log

## 2026-08-07 (start)
Did: Breakdown Milestone 1.5 dengan `planning-and-task-breakdown`, routing fase kerja lewat `using-agent-skills` (ci-cd-and-automation, observability-and-instrumentation, shipping-and-launch — bukan incremental-implementation seperti M1.2-1.4 karena tidak ada logic deteksi baru). Konfirmasi 4 keputusan teknis bersama user (platform Grafana, kanal notifikasi, sumber alert, cakupan dashboard). Tulis `decisions.md`.
Result: worked. Catatan penting: kanal notifikasi eksternal ditunda atas pilihan user — ini berarti Kriteria Keberhasilan #2 tidak akan terpenuhi penuh, dicatat eksplisit di depan (bukan ditemukan belakangan saat report.md).

## 2026-08-07 — Setup repo GitHub & GitHub Actions (Task 2)
Did: Cek `git remote -v` — ternyata repo lokal belum pernah dihubungkan ke GitHub sama sekali (padahal `gh` CLI sudah authenticated). Tanya user: buat repo baru vs pakai yang sudah ada vs tunda — user pilih buat baru dan public. `gh repo create Ardiyanto24/nirwana-database --public`, push `main`. Set `SUPABASE_DB_URL` sebagai GitHub Secret (`gh secret set`, value dari `.env` lokal, tidak pernah muncul di terminal/log). Tulis `.github/workflows/monitoring.yml` — cron harian (01:00 UTC) + `workflow_dispatch` untuk trigger manual, jalankan 8 script M1.2-1.4 secara berurutan, `.env` runner ditulis dari secret (tidak pernah di-commit). Push, trigger manual (`gh workflow run`), tunggu sampai selesai (`gh run watch`).
Result: worked — **run pertama sukses penuh, 10m15s, semua step hijau**. Diverifikasi data benar-benar masuk: snapshot volume baru (`created_at` 2026-08-07 04:39 UTC), `dq_test_results` bertambah untuk `run_date` hari ini. Catatan bukan-bug: jumlah baris `dq_test_results` hari ini (522) lebih besar dari 174 (jumlah expectation) karena `build_and_run.py` (M1.3) memang selalu INSERT baru per run tanpa dedup/upsert — akumulasi dari beberapa kali run manual saya + run GitHub Actions ini, di hari yang sama. Perilaku ini sudah ada sejak M1.3, bukan diperkenalkan GitHub Actions — relevan untuk desain dashboard nanti (agregasi per hari, bukan asumsikan 1 baris = 1 run).

## 2026-08-07 — Grafana Cloud & datasource (Task 1, 3)
Did: User daftar Grafana Cloud (14 hari trial Pro, otomatis turun ke Free tier permanen setelahnya — diverifikasi lewat WebFetch ke grafana.com/pricing, bukan diasumsikan). Buat service account + token, isi `GRAFANA_URL`/`GRAFANA_SERVICE_ACCOUNT_TOKEN` ke `.env` (`.env.example` diperbarui duluan sebagai dokumentasi format). Tulis `scripts/grafana/grafana_client.py` (helper API + parser `SUPABASE_DB_URL`) dan `create_datasource.py` (provisioning datasource Postgres via API, bukan klik manual).
Result: worked. `POST /api/datasources` sukses (id=13, uid=`cfufpsojqs5c0f`), `GET .../health` mengonfirmasi "Database Connection OK". Catatan: limit Free tier (10k metrics series, 50GB logs, 14 hari retensi) **tidak relevan** untuk setup kita — Grafana query langsung ke Postgres tiap load panel (live query), bukan data yang di-ingest/disimpan di Grafana.

## 2026-08-07 — Bangun dashboard 7 panel (Task 4)
Did: `scripts/grafana/priority_case_sql.py` (CASE expression prioritas, konsisten dgn `tables_config.py`), `build_dashboard.py` (7 panel tabel: Alert Aktif, Schema Drift Pending, Volume&Freshness 23 tabel, Ringkasan DQ per tabel, Detail Kegagalan DQ, Proporsi Dirty, Anomali IQR), push via `POST /api/dashboards/db`. Uji tiap panel langsung lewat `POST /api/ds/query` (bukan cuma asumsi dashboard tersimpan = query-nya benar).
Result: 7/7 panel query sukses (status 200), tapi ditemukan **2 kebocoran data simulasi ke dashboard production**:
1. Panel "Volume & Freshness" menunjukkan 26 baris, seharusnya 23 — investigasi: `monitoring.current_status` (view dari M1.2) tidak pernah memfilter `schema_name != '_simulation'`, jadi 3 tabel staging dari uji coba M1.2 (`volume_normal_case`, dst) ikut tampil seolah tabel production. Bug ini sudah ada sejak M1.2 dibuat, baru ketahuan sekarang karena baru ada consumer (dashboard) yang query `current_status` tanpa filter manual tambahan — waktu M1.2 sendiri diverifikasi, query manual saya selalu difilter ke 7 tabel prioritas Tinggi secara eksplisit sehingga kebocoran tidak pernah kelihatan. **Fix**: `scripts/monitoring/views.sql` ditambah `WHERE schema_name != '_simulation'` di 2 CTE (`latest_volume`, `latest_freshness`), view di-apply ulang, hasil `current_status` sekarang tepat 23 baris.
2. Panel "Schema Drift - Menunggu Review" menunjukkan 3 baris — semuanya ternyata `_simulation.staging_table` dari uji coba M1.4 (belum pernah dibersihkan sesuai catatan `report.md` M1.4 yang sengaja membiarkannya sbg bukti kerja). **Fix**: tambah `AND schema_name != '_simulation'` langsung di query panel (bukan ubah tabel `schema_drift_events` — datanya memang harus tetap ada sbg bukti, cukup query dashboard yang menyaringnya).

Setelah fix: redeploy dashboard (`version: 2`), re-test 7 panel — Panel Volume&Freshness = 23 baris, Panel Schema Drift = 0 baris (sesuai kondisi production nyata, tidak ada drift pending). Pelajaran: pola `WHERE schema_name != '_simulation'` yang konsisten dipakai di setiap script M1.2-1.4 untuk *menulis* data simulasi ternyata tidak otomatis diwarisi oleh view/consumer lain yang *membaca* data itu — perlu filter eksplisit di setiap titik baca baru, tidak cukup mengandalkan konvensi penamaan schema saja.

## 2026-08-07 — Alert rules Grafana (Task 5)
Did: `scripts/grafana/create_alerts.py` — buat folder "Nirwana Monitoring", 2 alert rule via `POST /api/v1/provisioning/alert-rules`: (1) "Alert Volume/Freshness/Kualitas Data (M1.2+M1.3)" — kondisi `COUNT(*) FROM monitoring.alerts WHERE is_simulated=false AND triggered_at > now()-1d > 0`; (2) "Schema Drift Menunggu Review (M1.4)" — kondisi `COUNT(*) FROM monitoring.schema_drift_events WHERE status='pending' AND schema_name!='_simulation' > 0`. Keduanya pakai expression Grafana `threshold` (bukan hitung ulang logic anomali, cuma cek "ada baris baru?" sesuai keputusan sumber alert).
Result: kedua rule berhasil dibuat (201), masuk rule group `data-production-monitoring` dengan evaluation interval 60 detik (default). State awal "unknown" (belum sempat dievaluasi saat dicek — wajar, rule baru dibuat). **Tanpa kanal notifikasi eksternal** (keputusan tertunda) — rule ini akan menunjukkan status Normal/Alerting di UI Grafana (Alerting > Alert rules), tidak push ke mana pun.

## 2026-08-07 — Verifikasi evaluasi & uji coba terkontrol (Task 6, 7)
Did: Cek state alert setelah 1 siklus evaluasi (60 detik) — tanpa intervensi apa pun, murni mencerminkan data production nyata yang sudah ada.
Result: **Alert Volume/Freshness/Kualitas Data (M1.2+M1.3) → `firing`** (benar — mencerminkan temuan nyata `bookings.total_amount` yang memang masih pending sejak M1.3). **Schema Drift M1.4 → `inactive`** (benar — 0 drift pending di production, sesuai laporan M1.4).

Did: Uji coba terkontrol aktif untuk rule M1.4 (yang kondisi awalnya inactive, perlu dibuktikan bisa bereaksi) — sisip 1 baris ke `monitoring.schema_drift_events` (schema/table nyata `corporate_master.properties`, tapi `column_name='_grafana_alert_test_column'` & `detail` ditandai eksplisit "BARIS UJI COBA", supaya jelas ini bukan drift nyata kalau ada yang lihat manual), tunggu 1 siklus evaluasi, cek state, lalu hapus baris itu, tunggu 1 siklus lagi, cek state final.
Result: **inactive → firing (setelah insert) → inactive (setelah dihapus)** — siklus penuh terbukti, rule Grafana bereaksi benar terhadap perubahan data di `monitoring.schema_drift_events` tanpa intervensi manual apa pun selain insert/delete SQL langsung. Ini membuktikan Kriteria Keberhasilan #2 bagian "muncul di dashboard" — bagian "terkirim ke kanal" tetap tidak terpenuhi (keputusan tertunda, kanal notifikasi eksternal).
