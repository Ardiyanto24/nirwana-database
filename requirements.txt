# Dependencies for scripts/monitoring/ (Milestone 1.2) and scripts/dq/ (Milestone 1.3)
psycopg2-binary==2.9.12
great_expectations[postgresql]==1.19.1

# Dependencies for scripts/extract/ (Milestone 2.1)
google-cloud-bigquery==3.27.0

# Dependencies for warehouse/ dbt project + scripts/mart_cleaned/ (Milestone 2.2/2.3)
# -- installed ad hoc locally before now, never pinned here since dbt only ran
# manually until Milestone 2.4's transform-mart-cleaned.yml needed it in CI.
dbt-core==1.12.0
dbt-bigquery==1.12.0

# Dependencies for scripts/data_scientist_access/ (Milestone 2.5)
pandas==3.0.2

# Dependencies for scripts/data_analyst_api/ (Milestone 3.4)
fastapi==0.115.6
uvicorn[standard]==0.34.0
