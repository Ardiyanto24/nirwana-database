# Kebijakan Akses Kredensial Scoped (RBAC Lapis 2)

**Ditulis untuk:** Milestone 2.6 — Isolasi Akses dan Kredensial Read-Only (`milestones/2.6-isolasi-akses-kredensial-read-only/decisions.md`).
**Cakupan:** Project-wide — seluruh kredensial scoped (service account BigQuery, role Postgres) yang dibuat sepanjang Fase 2, bukan cuma satu konsumen. Sesuai dokumen sumber (`docs/03-implementation-plans/02-serving-data-scientist.md` Milestone 2.6): RBAC lapis kedua ini "menjadi tanggung jawab pemilik infrastruktur data di seluruh sistem ini".

Dokumen ini beda dari `docs/02-requirements/rancangan-rbac-ai-chatbot.md` (Lapis 1 — RBAC level aplikasi chatbot, matriks `role_permissions`) — di sini murni soal **Lapis 2: isolasi kredensial di level database/warehouse**.

## Prinsip

Setiap kredensial baru yang dibuat untuk konsumen/job baru **wajib** least-privilege secara default:
- **BigQuery**: dataset ACL scoped ke SATU dataset (via `bq update` access list), bukan role project-level (`bigquery.dataEditor`/`bigquery.admin` di seluruh project) — kecuali ada alasan eksplisit yang didokumentasikan (lihat pengecualian `dbt-transform` di bawah).
- **PostgreSQL**: role dengan `NOSUPERUSER NOCREATEDB NOCREATEROLE`, grant eksplisit per skema/tabel (whitelist), bukan akses default yang lebih luas dari yang dibutuhkan.
- **Key file/password** dibuat manual oleh pemegang akses project (bukan disimpan di tempat yang bisa diakses otomatis oleh pihak lain), dan **tidak pernah** di-commit ke git (lihat `.gitignore` — `scripts/extract/*.json`).
- **Isolasi diverifikasi lewat uji coba nyata** (connect-as-role, cek query yang seharusnya diizinkan DAN yang seharusnya ditolak) — bukan diasumsikan benar dari niat desain saja. Pakai `scripts/bigquery_common/verify_dataset_isolation.py` untuk kredensial BigQuery baru (CLI generik, tidak perlu file baru per kredensial).

## Inventaris Kredensial (per 2026-08-10)

| Kredensial | Sistem | Scope | Milestone Asal | Bukti Isolasi | Least-Privilege? |
|---|---|---|---|---|---|
| `extract_reader` | PostgreSQL (production Supabase) | SELECT-only, whitelist 23 tabel eksplisit | M2.1 | `scripts/extract/setup_extract_role.py` (inline, verified saat dibuat) + re-verified M2.6 (query ad-hoc, tanpa rotasi password) | Ya |
| `extract-writer` | BigQuery (`nirwana-database-elt`) | Dataset ACL WRITER `raw_production` saja + `bigquery.jobUser` | M2.1 | Manual, dicatat di `milestones/2.1.../decisions.md` — tidak ada script re-runnable | Ya |
| `dbt-transform` | BigQuery (`nirwana-database-elt`) | **Project-level** `bigquery.dataEditor` + `bigquery.jobUser` | M2.2/2.3 | Manual, dicatat di `milestones/2.2.../decisions.md` | **Tidak** — pengecualian sadar (lihat di bawah) |
| `reverse_etl_writer` | PostgreSQL (serving project baru) | Schema-scoped `mart_cleaned` saja (CREATE/DROP/ALTER/RENAME table), `REVOKE ALL ON SCHEMA public` eksplisit | M2.4 | `scripts/reverse_etl/setup_writer_role.py` (inline) + re-verified M2.6 (query ad-hoc, tanpa rotasi password) | Ya |
| `reverse-etl-reader` | BigQuery (`nirwana-database-elt`) | Dataset ACL READER `mart_cleaned` saja + `bigquery.jobUser` | M2.4 | `scripts/reverse_etl/verify_reader_isolation.py` (re-runnable) + re-verified M2.6 | Ya |
| `data-scientist-reader` | BigQuery (`nirwana-database-elt`) | Dataset ACL READER `mart_cleaned` saja + `bigquery.jobUser` | M2.5 | `scripts/bigquery_common/verify_dataset_isolation.py` (re-runnable) + read-only dibuktikan (`CREATE TABLE` ditolak) + re-verified M2.6 | Ya |
| `ml-scoring-writer` | BigQuery (`nirwana-database-elt`) | Dataset ACL WRITER `ml_output` (tulis prediksi) + READER `mart_aggregated` (baca data fitur sumber, ditambahkan setelah gap ditemukan saat implementasi — mock scorer perlu baca `fact_revenue_room_type_daily`) + `bigquery.jobUser` | M5.4 | Service account + kedua ACL dibuat via `gcloud`/`bq` (dicatat `milestones/5.4-.../logs.md`); isolasi diverifikasi lewat `scripts/bigquery_common/verify_dataset_isolation.py` (`ml_output.predictions` allow, `mart_cleaned.financial_summary` deny — keduanya PASS) | Ya |
| `reverse-etl-mart-agg-reader` | BigQuery (`nirwana-database-elt`) | Dataset ACL READER `mart_aggregated` saja + `bigquery.jobUser` — nama dipersingkat dari `reverse-etl-mart-aggregated-reader` (35 char) karena GCP membatasi service account ID maks 30 karakter | M5.5 | Service account + ACL dibuat via `gcloud`/`bq` (dicatat `milestones/5.5-.../logs.md`); dipakai langsung produksi (sync 76 tabel real, 0 mismatch) sebagai bukti isolasi fungsional | Ya |
| `reverse_etl_mart_aggregated_writer` | PostgreSQL (serving project M2.4, schema baru `mart_aggregated`) | Schema-scoped `mart_aggregated` saja (CREATE/DROP/ALTER/RENAME table), `REVOKE ALL ON SCHEMA public` eksplisit — role TERPISAH dari `reverse_etl_writer` (M2.4/`mart_cleaned`) meski sama-sama di project serving yang sama | M5.5 | `scripts/reverse_etl_mart_aggregated/setup_writer_role.py` (inline, 4 cek termasuk eksplisit "SELECT mart_cleaned.* harus ditolak" untuk membuktikan isolasi antar-kredensial, bukan cuma dari `public`) | Ya |
| `revenue_analyst_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 8 view `analyst_views` + 2 tabel `mart_cleaned` (Revenue) | M3.5 | `scripts/data_analyst_credentials/verify_role_isolation.py` (re-runnable, generik) — allow cakupan sendiri, deny bypass `mart_aggregated` langsung + payroll + HR + F&B, write (INSERT) ditolak | Ya |
| `fnb_analyst_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 8 view `analyst_views` + 1 tabel `mart_cleaned` (F&B, termasuk `fnb_transactions` 902rb baris) | M3.5 | sama pola, PASS | Ya |
| `facility_analyst_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 9 view `analyst_views` + 1 tabel `mart_cleaned` (Facility/Ops) | M3.5 | sama pola, PASS | Ya |
| `spa_event_analyst_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 6 view `analyst_views` + 1 tabel `mart_cleaned` (Spa & Event) | M3.5 | sama pola, PASS | Ya |
| `hr_analyst_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 8 view `analyst_views` + 2 tabel `mart_cleaned` (HR) — **tanpa payroll**, diuji eksplisit 5 target payroll-adjacent (KK1 M3.5 worked example) | M3.5 | sama pola, PASS termasuk seluruh deny-check payroll | Ya |
| `corporate_financial_analyst_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 9 view `analyst_views` + 2 tabel `mart_cleaned` (Corporate/Financial, termasuk `payroll` — satu-satunya role dengan akses ini) | M3.5 | sama pola, PASS termasuk deny bypass `mart_aggregated.fact_financial_business_line_monthly` (paling kritis — membuktikan filter `Overall` exclusion M3.2 tidak bisa dilewati) | Ya |
| `property_gm_analyst_reader` | PostgreSQL (serving project) | **Role inheritance** (`GRANT <5 role domain> TO`), bukan grant objek langsung — mewarisi cakupan Revenue+F&B+Facility+Spa&Event+HR, TANPA Corporate/Financial | M3.5 | sama pola, PASS termasuk deny eksplisit ke `v_financial_business_line_group_monthly` (larangan tabel level-grup, M3.1 business rule #3) | Ya |
| `analyst-readonly` | BigQuery (`nirwana-database-elt`) | Dataset ACL READER **`mart_cleaned` DAN `mart_aggregated`** (2 dataset, beda dari kredensial BigQuery lain yang cuma 1) + `bigquery.jobUser` — TIDAK ke `raw_production`/`ml_output` | M3.6 | `scripts/bigquery_common/verify_dataset_isolation.py` (re-runnable, 4/4 OK) + read-only dibuktikan (`CREATE TABLE` ditolak, `403 Forbidden`) + akses terprogram lintas 2 dataset dibuktikan (`scripts/analyst_bi_access/example_query.py`) | Ya |
| `reservation_chatbot_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 10 view `chatbot_views` (Reservation) — **tidak ada grant ke `mart_aggregated`/`mart_cleaned` sama sekali** (lebih ketat dari kredensial analyst) | M4.3 | `scripts/chatbot_credentials/verify_role_isolation.py` (re-runnable, generik) — allow cakupan sendiri, deny bypass `mart_aggregated`+`mart_cleaned` langsung, deny `role_permissions`, deny cross-domain, write (INSERT) ditolak | Ya |
| `fnb_chatbot_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 11 view `chatbot_views` (F&B) | M4.3 | sama pola, PASS | Ya |
| `facility_chatbot_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 12 view `chatbot_views` (Facility/Ops) | M4.3 | sama pola, PASS | Ya |
| `spa_event_chatbot_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 9 view `chatbot_views` (Spa & Event) | M4.3 | sama pola, PASS | Ya |
| `hr_chatbot_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 10 view `chatbot_views` (HR) — **tanpa payroll**, diuji eksplisit 6 target payroll-adjacent | M4.3 | sama pola, PASS termasuk seluruh deny-check payroll | Ya |
| `financial_chatbot_reader` | PostgreSQL (serving project) | SELECT-only, whitelist 11 view `chatbot_views` (Financial, termasuk payroll — satu-satunya role dengan akses ini) | M4.3 | sama pola, PASS termasuk deny bypass `mart_aggregated.fact_financial_business_line_monthly` (business rule `Overall` exclusion) | Ya |
| `properties_ref_chatbot_reader` | PostgreSQL (serving project) | SELECT-only, 1 view `chatbot_views.v_properties_ref` | M4.3 | sama pola, PASS | Ya |
| `employees_directory_chatbot_reader` | PostgreSQL (serving project) | SELECT-only, 1 view `chatbot_views.v_employees_directory` | M4.3 | sama pola, PASS | Ya |
| `guests_pii_chatbot_reader` | PostgreSQL (serving project) | SELECT-only, 1 view `chatbot_views.guests_contact_view` (kolom kontak: `email`,`phone`) | M4.3 | sama pola, PASS termasuk deny silang ke `guests_profile_view` — pemisahan PII vs profil di atas 1 tabel fisik `guests` yang sama benar-benar tertegakkan di level kredensial | Ya |
| `guests_profile_chatbot_reader` | PostgreSQL (serving project) | SELECT-only, 1 view `chatbot_views.guests_profile_view` (kolom analitis: `loyalty_tier`,`nationality`) | M4.3 | sama pola, PASS termasuk deny silang ke `guests_contact_view` | Ya |
| `chatbot_authz_reader` | PostgreSQL (serving project) | SELECT-only, `mart_cleaned.role_permissions` **SAJA** — satu-satunya kredensial chatbot yang menyentuh `mart_cleaned` langsung (bukan `chatbot_views`), dipakai murni internal API M4.4 untuk keputusan otorisasi, hasilnya tidak pernah diteruskan sebagai response endpoint apa pun | M4.4 | `scripts/chatbot_credentials/verify_role_isolation.py`, PASS termasuk deny bypass `mart_aggregated`/`chatbot_views`/tabel lain di `mart_cleaned` (`guests`,`employees`) — role ini cuma bisa baca 1 tabel dari seluruh database | Ya |
| `chatbot_audit_writer` | PostgreSQL (**production Supabase**, bukan serving project) | INSERT-only, `monitoring.chatbot_query_log` **SAJA** — tidak ada `SELECT` sama sekali (bahkan ke tabelnya sendiri), tidak ada tabel lain — kredensial write pertama di daftar ini yang menyasar production Supabase, dipakai murni internal API M4.5 untuk menulis audit log tiap request | M4.5 | `scripts/chatbot_audit/verify_role_isolation.py` (copy pola generik, adaptasi allow=INSERT/deny=SELECT+UPDATE+DELETE karena arah kemampuannya kebalikan dari kredensial reader lain), PASS 7/7 termasuk deny SELECT ke tabel sendiri dan ke `corporate_master.role_permissions` | Ya — lebih ketat dari precedent monitoring existing (`scripts/monitoring/db.py`/`scripts/reverse_etl/connections.py`, keduanya masih pakai admin `SUPABASE_DB_URL` mentah untuk nulis `monitoring.*`) |
| `warehouse-monitor-reader` | BigQuery (`nirwana-database-elt`) | Dataset ACL READER **4 dataset sekaligus**: `raw_production`, `mart_cleaned`, `mart_aggregated`, `ml_output` (bukan `staging` — sengaja dikecualikan, staging cuma view, row count-nya selalu identik `raw_production`) + `bigquery.jobUser` | M6.3 | `scripts/bigquery_common/verify_dataset_isolation.py` (5/5 OK: 4 allow + 1 deny ke `staging`) + read-only dibuktikan (`CREATE TABLE` ditolak, `403 Forbidden`) | Ya — dipilih daripada reuse `dbt-transform` (project-level `dataEditor`) karena M6.3 murni baca row-count/timestamp, tidak pernah menulis; precedent scope-lintas-dataset: `analyst-readonly` (M3.6) |

### Pengecualian: `dbt-transform`

`dbt-transform` sengaja **bukan** dataset-scoped sempit — dbt-bigquery memanggil `bigquery.datasets.create` sebelum menulis model apa pun (idempotent, tapi tetap butuh izin ini dipanggil sama sekali), dan dbt di project ini mengelola **banyak dataset dari waktu ke waktu** (`staging`, `mart_cleaned_staging`, `mart_cleaned`, dan dataset baru di masa depan seiring pipeline bertambah) — scoped ke 1 dataset akan berarti mengedit ulang izinnya setiap kali ada dataset baru, bertentangan dengan alasan dbt dipilih (declarative, self-managing). Ini **trade-off sadar**, bukan kelalaian — dicatat eksplisit di `milestones/2.2-layer-staging-cleaning-per-tabel/decisions.md`.

## Siapa Boleh Memegang Kredensial Ini

- **Kredensial per-job** (`extract_reader`, `extract-writer`, `dbt-transform`, `reverse_etl_writer`, `reverse-etl-reader`, `ml-scoring-writer`, `reverse-etl-mart-agg-reader`, `reverse_etl_mart_aggregated_writer`): hanya dipakai oleh GitHub Actions workflow terjadwal (`extract-production.yml`, `transform-mart-cleaned.yml`, `reverse-etl-mart-cleaned.yml`, `scoring-occupancy-forecast.yml`, `reverse-etl-mart-aggregated.yml`) via GitHub Secrets, atau dijalankan manual oleh pemilik infrastruktur data untuk debugging/setup. **Tidak** dibagikan ke konsumen data (Data Analyst/Data Scientist/AI Chatbot) — mereka bukan pengguna akhir kredensial ini.
- **`data-scientist-reader`**: satu-satunya kredensial di daftar ini yang memang ditujukan untuk dipegang konsumen akhir (tim Data Scientist) — lihat `scripts/data_scientist_access/README.md` untuk cara pakai. Karena `mart_cleaned` memuat data sensitif penuh (PII, payroll — lihat `docs/03-implementation-plans/02-serving-data-scientist.md` "Prinsip Kunci"), permintaan salinan key file baru untuk anggota tim baru harus lewat pemilik infrastruktur data, bukan diteruskan bebas antar anggota tim.
- **7 kredensial `*_analyst_reader`** (M3.5): kredensial kedua yang benar-benar diserahkan ke konsumen akhir, satu per kelompok peran Data Analyst (Revenue, F&B, Facility/Ops, Spa & Event, HR, Corporate/Financial, Property/GM). Berbeda dari `data-scientist-reader` yang satu kredensial untuk seluruh tim, di sini **peran menentukan kredensial** — seorang analyst hanya menerima kredensial sesuai perannya, bukan kredensial admin bersama. `hr_analyst_reader` dan `corporate_financial_analyst_reader` memerlukan perhatian khusus saat permintaan salinan baru: HR menyentuh data personal untuk analisis turnover, Corporate/Financial menyentuh payroll penuh — keduanya sensitivitasnya lebih tinggi dari 5 role lain, permintaan harus lewat pemilik infrastruktur data. Dokumentasi lengkap per peran: `docs/08-serving-data-analyst/kredensial-analyst.md`.
- **`analyst-readonly`** (M3.6): kredensial ketiga yang diserahkan ke konsumen akhir — berbeda dari 7 kredensial `*_analyst_reader` (per-peran, PostgreSQL), ini **1 kredensial dipakai bersama seluruh tim Data Analyst** untuk akses BigQuery langsung (mirip pola `data-scientist-reader`, tapi scope 2 dataset bukan 1). Tidak ada pemisahan per-peran di level BigQuery — isolasi domain (HR tidak lihat payroll, dst) tetap ditegakkan di lapisan PostgreSQL (M3.5), BigQuery langsung sengaja dianggap kebutuhan "power user" lintas-domain untuk analisis lanjutan. Cara pakai: `scripts/analyst_bi_access/README.md`.
- **10 kredensial `*_chatbot_reader`** (M4.3): satu per `data_domain` RBAC AI Chatbot (Reservation, F&B, Facility/Ops, Spa & Event, HR, Financial, Properties Ref, Employees Directory, Guests PII, Guests Profile) — **bukan** dipegang manusia langsung seperti kredensial analyst, melainkan dikonsumsi oleh layer API chatbot (Milestone 4.4, `scripts/chatbot_api/`) yang memilih kredensial mana dipakai per request berdasarkan `role_permissions` persona yang sedang chat. Lebih ketat dari kredensial analyst: tidak ada grant apa pun ke `mart_aggregated`/`mart_cleaned` mentah, seluruhnya lewat view `chatbot_views` yang sudah mengkurasi kolom. `guests_pii_chatbot_reader` dan `guests_profile_chatbot_reader` perlu perhatian khusus — keduanya mengakses tabel fisik `guests` yang sama lewat 2 view kolom berbeda, permintaan salinan baru harus lewat pemilik infrastruktur data. Dokumentasi lengkap: `docs/09-serving-ai-chatbot/kredensial-chatbot.md`.
- **`chatbot_authz_reader`** (M4.4): kredensial ke-11 chatbot, beda kategori dari 10 di atas — bukan data konsumen, murni infrastruktur internal API (`scripts/chatbot_api/authz.py`) untuk mengecek `role_permissions` sebelum query data apa pun dijalankan. Tidak pernah dipegang siapa pun di luar proses API itu sendiri — hasil query lewat kredensial ini tidak pernah keluar sebagai response HTTP. Dokumentasi: `docs/09-serving-ai-chatbot/api-chatbot.md`.
- **`chatbot_audit_writer`** (M4.5): kredensial ke-12 chatbot, juga infrastruktur internal murni (`scripts/chatbot_api/audit.py`) — tidak pernah dipegang siapa pun di luar proses API. Beda fisik dari 11 kredensial chatbot lain: menyasar **production Supabase** (`monitoring.*`), bukan serving project, karena log audit chatbot ikut konvensi "monitoring stays centralized" (lihat CLAUDE.md) alih-alih lokasi fisik API-nya sendiri. Dokumentasi: `docs/09-serving-ai-chatbot/audit-log-chatbot.md`.
- **`warehouse-monitor-reader`** (M6.3): kredensial per-job seperti kelompok pertama di atas (bukan dipegang konsumen manusia) — dipakai `scripts/monitoring_warehouse/` (snapshot volume BigQuery, freshness `ml_output`) via GitHub Secret di workflow terjadwal `monitoring-warehouse-dq-anomaly.yml`, atau dijalankan manual oleh pemilik infrastruktur data. Satu-satunya kredensial BigQuery yang scope-nya lintas **4** dataset sekaligus (lebih luas dari `analyst-readonly`'s 2) — tetap READER murni, tidak ada grant tulis ke dataset manapun.

## Proses Meminta Kredensial Baru

1. Tentukan scope minimum yang benar-benar dibutuhkan (dataset/schema mana, read atau write).
2. Ikuti pola di atas: `bq update` dataset ACL (BigQuery) atau `CREATE ROLE` + grant eksplisit per schema (Postgres) — bukan role project-level/superuser kecuali ada alasan sekuat `dbt-transform` di atas, dan itu pun harus didokumentasikan eksplisit di `decisions.md` milestone terkait.
3. Verifikasi isolasi lewat uji coba nyata sebelum kredensial dipakai produksi — pakai `scripts/bigquery_common/verify_dataset_isolation.py` untuk BigQuery, atau `scripts/data_analyst_credentials/verify_role_isolation.py` untuk Postgres (CLI generik sejak M3.5, dipakai ulang 7× untuk kredensial analyst — sebelumnya tiap `setup_*_role.py` Postgres punya verifikasi inline sendiri-sendiri; pola lama itu masih valid untuk kredensial non-analyst, lihat `scripts/extract/setup_extract_role.py`/`scripts/reverse_etl/setup_writer_role.py`).
4. Key file/password dibuat manual oleh pemilik infrastruktur data (atau via `gcloud`/SQL langsung), tidak pernah di-commit — tambahkan pattern-nya ke `.gitignore` kalau path baru.
5. Tambahkan 1 baris ke tabel inventaris di atas.

## Rotasi dan Pencabutan

**Gap yang diketahui (belum diselesaikan milestone manapun sejauh ini):** tidak ada rotasi terjadwal otomatis untuk key file/password kredensial mana pun di daftar ini — sama seperti dicatat di `milestones/2.5-api-akses-data-scientist/report.md` Known Gaps. Rotasi manual sejauh ini hanya terjadi insidental (mis. re-run `setup_extract_role.py` yang otomatis rotasi password tiap kali dijalankan ulang — efek samping yang **harus disertai update GitHub Secret terkait**, atau workflow terjadwal akan gagal koneksi).

Untuk mencabut akses (mis. anggota tim Data Scientist keluar): pemilik infrastruktur data membuat key baru (`gcloud iam service-accounts keys create`) dan **menghapus** key lama (`gcloud iam service-accounts keys delete <KEY_ID>`) — bukan menghapus service account itu sendiri (akan mematahkan workflow lain yang mungkin ikut memakainya, kalau ada).
